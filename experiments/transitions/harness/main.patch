diff --git a/benchmark/fs-bench-pro/src/main.rs b/benchmark/fs-bench-pro/src/main.rs
index 5cf6ff14c..ec5c57f02 100644
--- a/benchmark/fs-bench-pro/src/main.rs
+++ b/benchmark/fs-bench-pro/src/main.rs
@@ -18,6 +18,7 @@ mod repository_init;
 mod sdk_edit_verify;
 mod sdk_file_edit;
 mod storage_smoke;
+mod transition_experiment;
 mod workspace_bench;
 mod workspace_reliability;
 mod workspace_sequence;
@@ -671,6 +672,12 @@ fn run() -> AnyResult<()> {
     }
     let _commit_diagnostics = layerfs_sdk::capture_workspace_commit_diagnostics()?;
     let args = std::env::args_os().skip(1).collect::<Vec<_>>();
+    if args
+        .first()
+        .is_some_and(|arg| arg == "transition-experiment")
+    {
+        return transition_experiment::run(&args);
+    }
     if args.first().is_some_and(|arg| arg == "repository-init") {
         return repository_init::run(&args);
     }
