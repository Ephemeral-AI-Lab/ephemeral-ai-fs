# R19 fast namespace Init optimization lane

The user now explicitly requests fast namespace Init speed iterations and defers
independent verification and full157 until terminal optimization. This supersedes
the R6/R15/R16/R17 per-iteration four-cell/proof cadence. No full157 run has begun.
Retain completed checks and observations; do not rerun them during this lane.

Use the existing namespace100000 input recipe/digest and public native Init path,
4KiB pages, native storage, original canonical/authentication/resource/publication
contracts. No new fixture or weakened product implementation. Each proposed source
gets one selected performance observation in a fresh owned output Store; keep all
failures and unfavorable observations. Use the shared measurement lock. Reuse one
build location, pristine fixture and compatible artifacts rather than rebuilding
unaffected runtimes. Save exact command, source/dirty patch hash, binary hash,
profile, raw elapsed/phase/resource counters and cleanup before comparisons.

A host-only diagnostic may use an existing native Init entrypoint, with its exact
route/timer recorded and separated from historical full-harness observations.
Any faster build profile must be separately named and compared only to the same
profile. No diagnostic observation is final qualification. At terminal optimization,
freeze the candidate and run required correctness, independent/public-operation
verification, resource/storage and full157 gates, preserving unfavorable evidence.
