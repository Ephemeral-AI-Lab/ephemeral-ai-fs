# R10 prospective residual profile after redundant-index removal

G4 clean1658f1b76 records namespace1000006,761,617,125ns and payload500
3,823,728,334ns; all four R6 independent proofs pass. These still fail to restore
published v0.1.3 performance; retain them. The first two repairs removed distinct
scratch/ownership costs but do not explain the remaining admission wall.

Use the existing R7 profiler orchestration against sealed G4 only: one selected
namespace100000 seed1 public performance operation, one-second delay after our
infra-run process appears, three-second macOS sample at1ms, then unchanged
independent proof. Original fixture/state/timers and300/310/600 and45/59 budgets.
This instrumented observation is diagnostic-only and cannot replace G4 timing.
No extra build, concurrent Store writer, shape change, codec change, memory or
transaction-limit increase. Inspect main-thread SQL lookup/insertion/commit,
canonical authentication, native context/encoding, grouping and producer stalls
before choosing a further bounded repair. Existing storage savings remain required.
