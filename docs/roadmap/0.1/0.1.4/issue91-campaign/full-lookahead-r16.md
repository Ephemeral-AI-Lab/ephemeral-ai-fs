# R16 prospective bounded FULL-frame lookahead

Step 1 is frozen as G8 source93568b10c. Its isolated build and R15/R6
observations are pending when this declaration is written; no outcome is inferred.
G7 remains the last observed product comparison, and published v0.1.3 remains
the qualification baseline. Preserve every earlier observation and failure.

At an existing admission batch boundary, overlap Store-free native FULL encoding
of the already-probed missing incoming-page tail with owner-thread publication.
Only payloads without predecessor provenance or hints qualify. Keep the existing
canonical batching, native FULL/PREFIX decisions and bytes, pack layout, absence
epoch, positive collision authentication, external dependency closure, 4KiB pages,
transaction limits, synchronous durability, and rollback contract.

Use at most one joined CPU worker. It owns no Store/session and cannot publish or
roll back. Join it before resolving publication or worker failure. Carry frames
by owned object identity through the ordinary/final batch; never enlarge canonical
objects or queues. Charge actual live capacities for prepared packs, publication
associations, the 1MiB encoder, temporary output and carried frames against the
existing aggregate 2MiB physical and 6MiB data ceilings. Insufficient headroom or
a stale absence proof selects the original synchronous path. Discard speculative
frames before preparing any batch with predecessor provenance/hints, and discard
optional frames rather than changing any original codec reservation or decision.

Before qualification, preserve a failing focused check and then demonstrate exact
FULL/pack bytes, real worker/publication overlap, bounded fallback, final-batch
ownership, and joined-worker cleanup after publication failure. Report eligible,
computed, consumed/discarded frames, overlap and worker overhead, and actual peak
ownership. A worker existing is not evidence of useful overlap.

Reject this treatment if unchanged live bounds leave no useful eligible tail,
codec/layout/storage choices change, authentication/rollback proof fails, CPU
work does not overlap publication, or overhead worsens the unchanged headline
observations. Keep the rejection evidence and remove the product treatment rather
than ship nominal concurrency. Do not substitute a larger queue, a second SQLite
connection, a new allowance, or changed PREFIX policy to rescue this experiment.

After checks, the coordinator alone freezes a distinct candidate source/binary/
image and executes the same four R6 cells, order, seed1, public timers, limits and
independent proofs once. No concurrent build/measurement. Compare to observed G8
when available, G7, and the published baseline without replacing any sample or
claiming statistical speedup. Final affected matrix, full157 equal-state storage,
independent review and every existing qualification obligation remain required.
