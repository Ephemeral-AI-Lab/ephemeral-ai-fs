# R3 second prospective profiling attempt

The first profiler captured the outer infra-run process, whose samples were all
Command::status / wait4. This is invalid stack attribution, not a product wait
root cause. Retain profile1, its sampler exit0, valid diagnostic4.778606001s
observation and proof separately; do not replace original G05.275265667s.

One corrected G0 diagnostic uses the identical directory-content-scan500/seed1
workload and300/310/600 limits. Identify the unique exact infra-run wrapper,
then its direct child with the same sealed binary and command:
`workspace-run SAMPLE_ROOT/payload SAMPLE_ROOT/payload/input CASE 1 performance CONTAINER`.
Require exact parent PID, sample root, case/seed and container match. Record both
processes and run sample CHILD_PID3seconds1ms once. No wrapper fallback or
broad name match. Retain full profile/raw receipt, followed by matching ordinary
independent verification. This remains observer-contaminated diagnostic evidence,
not a new qualification sample. Choose a read repair only from actual worker
stacks and scoped counters. Host profiles cannot attribute Linux CPU by themselves.
