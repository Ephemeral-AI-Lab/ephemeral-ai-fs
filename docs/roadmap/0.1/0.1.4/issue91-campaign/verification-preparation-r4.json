{
  "schema": "issue91-proof-preparation-v1",
  "generation_applicability": "Subsequent sealed generations only; original G0 failures retained",
  "cases": [
    "namespace-100000",
    "store-footprint-unique-100000",
    "store-footprint-metadata-cardinality-100000"
  ],
  "reason": "G0 verifier cache misses consumed25-27s before required product work and hit the unchanged41s internal work end. Existing QUICKSTART permits explicit preparation before proof. Run existing prepare-only immediately before these proofs, then unchanged45s work/59s hard verifier.",
  "setup_timeout_seconds": 600,
  "verification_work_seconds": 45,
  "verification_hard_seconds": 59,
  "recording": "Separate immutable preparation receipt and command/wall, plus original verifier wall and their disjoint end-to-end sum. Do not compare the warm verifier wall to historical cold inclusive wall as a speedup. Preserve all failed G0 attempts. Product Init regressions still require repair.",
  "operation_contract": "No changed input, oracle, SDK operation, timer or resource bound. Existing authenticated compatible cache acquisition; fresh writable proof ownership. This schedules only three demonstrably expensive fresh-output preparations."
}
