# Combined control: C plus fixed S1

Prospective implementation contract, before source changes, builds or samples.
C continuation requires all157 verifier/cleanup and158-row authenticated cohort
PASS. Retain C only as the research foundation: its historical Commit sum rose
72.12%, despite a100655104-byte acknowledged allocation reduction. This does not
accept the current legacy matcher cost for production.

Port only the reviewed existing S1 mechanism into this isolated control checkout:
default ObjectStore origin hook, tree Engine persist handoff, exact inode-table
leaf ObjectBuffer origin, and same-role selected-FULL admission policy. Keep the
C1073741824-byte operation allowance and all other budgets, diagnostic fields,
canonical encodings/IDs, group/pack codec, reader, SQL and transaction semantics.
The attached integration review specifies exact method boundaries and source
commits. Existing source-only S1 tests are reused with D file-cohort exclusions.
No native wire2 code enters this control. No original Store or evidence changes.

The next matched public comparison is fresh C+S1 legacy control versus the same
foundation plus the prospectively declared native payload physical treatment.
It is not an additional C-versus-S1 factorial campaign. Freeze source/product/
dirty seals, host/image/codec and exact normal commands before samples. Use one
fresh pair for each approved smoke in DeepSeek-five, frequent-edits, small-files
order; control then candidate, each with normal independent verification and
cleanup. Preserve all negative values. A written applicability/cost decision is
required before a fixed full157 pair. No sample or build launches from this note.

Use the existing shared measurement lock; two build jobs,900s build; original
2CPU/2GiB/no-swap/256PID containers with host Store/SDK/spool. Keep300s public
steps,4h phases,120s setup/cleanup,16GiB Store/scoped runtime,32GiB outputs,
50GiB host-free and8GiB sampled host RSS ceilings. No sweep, parameter change,
cleanup/oracle waiver, migration, rollout, M5/cloud work or merge.

Storage account is nested and based on final original acknowledgement plus one
preverification logical snapshot. Reuse one authenticated census per final Store.
Measure payload/structural groups and actual bases; do not subtract the historical
S1 reduction from C as a forecast. Historical full-run timing is not a fresh pair.
159163199 encoded bytes and134221004 allocated bytes retain their distinct scope.
