# Independent prospective C source and contract review

Reviewed the complete uncommitted C diff and `contract-v1.md` in the isolated coverage checkout before any reviewer build or sample. The parent reports completed D all157 verification/cleanup and unique158-row graph validation PASS; that completes the prerequisite rather than implying C has already succeeded.

**Source approval for the scoped research build:** the production diff introduces one named1073741824-byte cumulative correspondence-operation constant and substitutes it for the16777216-byte literal in the existing checked atomic reservation predicate. No other16MiB allowance, custom matcher-work limit, codec, group/pack format, predecessor choice, CAS order or memory decision changes. The diagnostic saturation test now computes the final admissible whole grant from the actual cap, preserving its original purpose; a new arithmetic test checks the declared workload bound and rejection beyond the finite endpoint. Tests have not been executed by this reviewer.

The arithmetic is exact:3763×2×131136=986929536 B;1GiB leaves86812288 B headroom, approximately8.80%. The cap permits8188 whole grants, equivalent to4094 two-read cursors. A grant beyond8188 would exceed the cap because131136 does not divide1GiB. This is a cumulative logical-work policy, not1GiB resident allocation.

The two-read argument is correctly workload-specific: the actual predecessor population must consist of FileState roots with one extent leaf and no extent branches. Source takes the FileState root once, reads its mapping leaf once and retains its iterator. Attached-but-unused or skipped-beyond-EOF cursors consume less. A different prior graph, captured/staged source or future workload can invalidate this demand estimate; the new constant is not a theorem of global complete coverage. The contract explicitly conditions its prediction on the same flat-root/cursor population and preserves all per-file/descriptor/memory limits. Continued exhaustion is a negative result, not authority to raise the cap again.

The64× policy increase can allow up to269994 grants across the D-observed134997 cursors, about13.75× D's19638 successful grants. The contract discloses this workload increase and requires actual read/CPU/memory/allocation/public cost measurements. It does not assume the489185498 canonical bytes lacking hints will compress well, nor label reuse-triggered reservations wasted.

The prospective execution design is appropriately finite: three fresh D/C development smoke pairs, unchanged oracles/workloads and exact identities, then an explicit written usefulness/cost decision before any full157 C run. Correctness alone does not launch full history. The roughly30% owner guidance is preserved without automatically accepting larger slowdowns or inventing a new gate. Unfavorable values, continued fallback, baseline fault-test failures and incomplete/failed attempts remain visible. Full157 comparison to historical D is explicitly not a fresh timing pair.

No blocking source or contract contradiction was found. Proceed to the parent-owned serialized focused tests and identity freeze; that approval is not a C performance or product-adoption verdict. C still must pass run-level provenance, diagnostics, oracle, resource and cleanup checks. Native-prefix P and S1 remain excluded from this one-variable treatment, and the combined milestone remains unmeasured.

Parent refinement before execution: removed the new arithmetic-only unit test; the existing real-cursor saturation test still derives and exercises the actual ceiling. Bound arithmetic remains explicit in the prospective contract. Production change is identical.
