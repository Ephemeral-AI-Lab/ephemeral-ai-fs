# Independent C smoke promotion review

Read `issue88-C-custody-1/smoke-results.json` only; no Store, build, benchmark, decoding or bulk hash work. **One unchanged C full157 applicability run is justified**, after the parent freezes this cost/usefulness decision and exact identity. This is promotion for measurement, not acceptance of C as a production default or a waiver of numerical regressions.

All six case rows across three approved smoke selectors report performance/verification/cleanup success, covering33 mappings per arm. Initial-state verification entries are correctly separate from performance operations. The parent reports all140 marginal-counter checks pass. These are single fresh D/C development pairs, not final three-pair qualification.

DeepSeek-five demonstrates the intended mechanism: operation-limited empty target bytes1718547→0, grants508→890, admitted file-payload DELTAs266→478. Logical SQLite3158016→2772992 falls385024 B (94 pages), with equal final canonical population8980067 B/5557 objects. This is stronger evidence than a favourable elapsed fluctuation or an allocation-only change: newly supplied hints produced additional admitted deltas and lower logical representation size.

Do not headline the1044480 B allocated reduction as encoded savings. Allocated4202496→3158016; D adjustment4202496−3158016=1044480, C adjustment3158016−2772992=385024. Thus allocated reduction1044480 =logical reduction385024 +signed-adjustment reduction659456. The residual mechanism is not established or forecastable.

The important cost is Commit, not only the combined public sum. Total Commit283939250→351705291 ns increases67766041 ns (about23.87%). Step3 rises58731792→89128208 ns: +30396416 ns/+51.75%, the largest relative regression. Step5 rises66379209→97788708 ns: +31409499 ns/+47.32%, the largest absolute increase. Step2 also increases about43.48%. The combined Exec+Commit sum1333454791→1270494582 ns happens to fall, but this does not establish a faster encoding path; Exec variations can mask added Commit work. Host public-phase CPU418144458→474234621 ns rises about13.41%.

SDK-text operation sum36755999→40396627 ns increases about9.90%, with unchanged grants and final logical/allocation. Other frequent-edit operation sums range roughly−6.7% to+3.6%, with unchanged grants. Small-files logical and allocated208896→212992 rise4096 B despite a slightly lower combined operation sum; preserve that negative result. Cases without an active operation limit do not establish usefulness of raising it, and their timing variation should not be assigned to extra traversal that did not happen.

## Why one full run is warranted

The targeted smoke removes observed coverage exhaustion and obtains a concrete logical-storage benefit with increased but bounded absolute Commit cost. The D full157 diagnosed a much larger489185498 canonical-byte operation-limited cohort. One fixed full run can determine whether C's prospective topology bound removes that bottleneck at history scale, what unchanged downstream limits become active, and whether additional encoded/allocated benefit justifies public CPU/read/Commit costs. The smoke cannot forecast that result or prove near-linear cost scaling.

Freeze the existing C identity and1073741824-byte allowance unchanged. Retain all original per-file, descriptor, matcher/read/trial, context, operation deadline and resource/cleanup limits. Do not increase another limit if newly supplied hints expose it. Compare full157 against historical D descriptively; obtain no fresh paired-speedup claim from this continuation. Preserve a pre-verification logical snapshot and verify all157 states before a final disposition.

The owner’s approximately30% guidance is considered explicitly: aggregate Commit increase is below that comparison point, while individual steps exceed it by meaningful absolute tens of milliseconds. Neither fact automatically accepts or rejects research. Record both before the full run. Stop on correctness, provenance, diagnostic conservation, resource or cleanup failures; preserve a timeout or adverse cost result without extending budgets. A completed full run may still reject C for adoption.

This decision does not schedule another smoke pair, a parameter sweep, CAS deferral, S1 enablement or native-prefix P. C's result will determine the single frozen delivery foundation used by both later S1-only and combined arms.
