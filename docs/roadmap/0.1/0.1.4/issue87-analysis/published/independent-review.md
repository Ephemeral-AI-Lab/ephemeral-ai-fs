# Independent custody and root-cause review — issue #87

Reviewer: independent `/root/review` agent, 2026-09-08. Scope: sealed receipts, source and shared inventory outputs. This reviewer did not open the Store, run a second census, replay, encode alternatives or change product files. All numeric bytes/ns/counts below are integer observations or explicit derived arithmetic; claims refer to the full157 M4.5 run unless stated otherwise.

## Custody independently checked

The final full157 contract, applicable benchmark/AGENTS.md, benchmark rules and QUICKSTART were read in full. Issue #87 was retrieved in full. The expected old final report `docs/roadmap/0.1/0.1.4/deepseek-full-157-m45.json` and `builds/full157-physical-census.json` were absent at discovery; the existing complete `builds/full157-report.py` was reviewed as source, not treated as executed evidence. Therefore this task creates the completed analysis from original sealed evidence rather than authenticating an imaginary earlier report.

- The verification manifest has 802 entries; every current hash matches. The performance manifest has 324 entries; exactly the Store differs. All other 323 entries, including retained branch/layer identity files, match. The original performance Store hash is preserved; its missing bytes cannot be reconstructed from the post-verification Store.
- Producer binary SHA256 `d3d6bdb3166f8d3211cf99b178e0226e6963291bb7ab7b60dc32cd033d2d140b` independently matches the retained binary and host identity. Producer revision `452158572b72adb4a480740dbe13fe36e8338491`, dirty=true, tree `8cf7f76cae81952f83ab94b5a99f30d659d4c208` differs from clean harness checkpoint `014c0b9cb5d62bd50ded2052a2604d4aeab76dce`, tree `cf7cb08126808c9296aec985bb1b600966818883`.
- Preserved dirty-patch SHA256 `8019305d565dd1784d641064d864afc25efdeb96894b8b4e39c6a225e4e79728` is byte-for-byte the Git diff between those revisions. It changes three benchmark coordinator files and no product crate. Independently recomputed source seal `9406b4e21f9b21973e7675cd07931430c3bbee87e32daf0fa46af995cdf4a329` and product seal `5407203d4fe7adba5c4aa4c8222e825b24192a17ac3b726adce3d97d769f270f` match producer records. Equivalent source content does not mean the binary was built from the reporting commit.
- Retained Docker image `sha256:8e609f2bd9e304fe3c36ef61b0c7771275b5247f987aeb9b79946ad34fb59ed6` exists; actual inspected labels match source/product/workload seals and dirty producer revision. Recorded performance/verifier environments have 2 CPUs, 2147483648-byte memory and equal memory+swap limits, 256 PIDs, `/dev/fuse`, and no data-sharing mounts.
- Frozen manifest hash `03f21acfb415907f521217e7a972ed512265c8d0c2da0f8034e2ff3014334271` matches current immutable input manifest. All 157 identity/performance/frozen source indices, SHAs and trees agree, and all verifier identities match corresponding performance mappings. All current oracle files and input manifest files match their recorded hashes. Independently parsing all157 observed TSVs, rejecting duplicate paths, reproduces each complete oracle dictionary exactly. All seven original historical raw file hashes match the control custody file. All 157 commits were Created. Verifier reports 4936693030 bytes and 904143 entries. Both phases report PASS, cleanup PASS and container_removed=true.
- Four historical published JSON hashes independently match immutable documentation commit `1c7c9235115d1b4f21bc2eae7af822552b7be3ed`: checkpoint manifest, LayerFS results, Git control results and Git mapping. Historical 940310528-byte LayerFS and 56373248-byte delta-packed Git allocations have equal selected157-tree scope. Git compression6/window10/depth50/threads2 packing and construction costs are separate; metadata, API, topology and timing differences remain. The old verifier independently checked paths/types/content/modes/symlinks, not normalized mtimes.

## Conservation and scope challenges

1. **Acknowledgement vs current stat:** primary allocation335552512 B and logical318832640 B imply signed adjustment+16719872 B. Current post-verification logical318869504 B is 36864 B (9 pages) larger. Its current allocation326438912 B and adjustment+7569408 B cannot replace acknowledgement values or prove a reclamation mechanism. No pre-verification page layout survives.
2. **SQLite partition:** shared physical inventory balances318869504 B =299909120 object_packs pages +18837504 objects-index pages +122880 other metadata pages. BLOB295944702 B is nested in SQL payload295969063 B, not additive; row encoding24361 B is the difference. Payload311738751 +unused5588767 +overhead1541986 equals page bytes. Zero freelist/residual is measured/derived from validated page inventory, not a substitute for unknown values. Overflow belongs to its owner; no extra overflow additive category is used.
3. **Allocation attribution:** neither unused bytes nor signed allocation adjustment is measured recoverable. Even complete elimination of these small accounting buckets cannot explain the279179264 B allocation gap to historical packed Git; subtracting differently timed snapshots is not an exact decomposition of that gap.
4. **Times:** performance case work460033711875 ns and case wall463745728334 ns differ from enclosing invocation540947041625 ns, which includes preparation76762192750 ns. Historical performance_phase644755990000 ns excludes its separately reported preparation51964027833 ns. No speedup is inferred from those incompatible enclosing boundaries. Codec/matcher durations are nested work and are not added to public elapsed. Observer wall/cache perturbation remains outside public timers but inside broader case observations.
5. **Counter populations:** observed phase aggregates86417 eligible targets,77949 targets_without_hints,18589 absent_predecessors,11293 usable bases/trials,8024 DELTA admissions and358117 FULL admissions have different populations. No-hint and absent-predecessor counts overlap.125254 budget skips are all correspondence events recorded per predecessor cursor/file or unavailable reservation, not unique post-CAS targets; fetch/match/instruction/memory skip counters are zero. Prepared full/mixed/selected encoded-byte counters can contain work losing final admission; they are not automatically durable savings. Final selected locators, not pack-ID order, establish durable representation.

## Source mechanisms and challenged hypotheses

Every row follows symptom → affected bytes → mechanism → alternative → discriminating evidence → smallest candidate boundary. Affected bytes are ceilings, not predicted savings.

| Hypothesis | Disposition and causal review |
|---|---|
| Small-file structural overhead dominates | **Contradicted as principal explanation.** Authenticated structural canonical bytes94362335 B are11.8% of799525289 B; payload705162954 B dominates. Homogeneous payload groups alone216448341 encoded B exceed all77593027 mixed-role encoded B plus1199510 homogeneous structural encoded B. SQL metadata122880 B and objects index18837504 B cannot explain the whole gap. Structural canonical bytes must be authenticated after outer framing, distinguishing FileState and extent codecs. Alternative is payload-dominated groups. Exact decoded roles and homogeneous/mixed encoded groups now establish this disposition; mixed bytes are not proportionally assigned to records. A versioned canonical simplification is justified only if one removable structural bucket is material. |
| SQLite placement dominates | **Contradicted as principal explanation by measured scope.** Most logical bytes are pack BLOB295944702 B; total post-verification logical bytes outside BLOB22924802 B, plus independently scoped acknowledgement allocation adjustment16719872 B, are too small to explain279179264 B gap. Unused5588767 B is not proven recoverable. Alternative page and filesystem mechanisms remain unspecified. Smallest future boundary would be read-only placement diagnosis, not VACUUM/backend replacement. |
| Missing hints suppress useful deltas | **Source-supported inference of missing coverage; savings unavailable.**77949 no-hint events among86417 eligible counts; the final eligible-shape payload set contains705162954 canonical B, but target-outcome byte weights are unknown. `DeltaSearch::candidate` accepts validated chunk framing and returns FULL fallback without prior IDs. `PredecessorCursor` uses bounded descriptors/node reservation. `put_file_payload` attaches first_span; both memory-owned delivery and spill delivery preserve typed hint metadata. `consume_prevalidated_pages` marks predecessor existence independently of span presence and invokes correspondence only when both exist. A missing required span must therefore be a distinct defect outcome; source review does not prove one occurred in this run. Alternative: new payload/no overlap or useless predecessor data. Unique post-CAS target bytes by mutually exclusive terminal reason discriminate. Smallest next boundary is fixed aggregate byte-weighted funnel diagnostics, without changing hint/matcher policy. |
| Matching limits are too restrictive | **Contradicted for recorded budget-loss claim; heuristic quality remains hypothesis.**11293 trials,0 measured match/fetch/instruction/memory budget skips. Source uses four seed positions per4096 buckets,16-byte seeds and bounded batch work. A weak heuristic may miss matches without exhausting budgets; low8024 DELTA count does not prove this. Bytes of useful candidates lost to specific limits are unavailable. Only byte-weighted evidence on unchanged usable bases could justify one matcher parameter change. |
| FULL anchors dominate | **Contradicted for extra-anchor dominance.** All366141 selected objects are in logical union L (799525289 canonical B); B-minus-L has0 objects/0 canonical B. FULL count358117 includes logical payload/structure, not extra anchors. Depth-one policy admits only selected authenticated FULL anchors and can follow a prior DELTA to its FULL anchor. Alternative is anchors already retained logically, costing no additional objects. Authenticated B-minus-L is empty; canonical base bytes are not group reclaimability. Anchor-policy change only if extra base-only groups are material. |
| Mixed-group compression rejects good raw deltas | **Observed rejection, material-loss claim unavailable.**332 rejected mixed groups, affected target/group bytes unknown. `encode_group` requires compressed improvement at least max(64,ceil(FULL/8)); raw candidates alone are insufficient. Alternative: marginal or compression-hostile DELTA programs. Complete attempted A/B by group and persisted winner populations discriminate. Smallest boundary is candidate/group diagnostics, never automatically deeper chains. |
| Residual content is mostly genuinely new | **Unproven.**18589 absent-predecessor events are overlapping target counts with unknown canonical bytes; CAS misses mean different bytes, not incompressible or unrelated bytes. Initial snapshot payload has no predecessor by definition. Cross-version resemblance and byte-weighted reasons discriminate. Compression/construction applies only if initial unrelated payload dominates; predecessor search cannot explain that category. |
| Repeated decoding constrains stronger encoding | **Source-supported repeated work; causal ceiling unavailable.**869798 decompression calls,880813 group fetches,12450972177 decoded-read bytes and7309561533 encoded-read bytes are interval counters, not unique disk reads. Encoding2566416665 ns and matching5677066532 ns are nested. Alternative causes include traversal/COW/verification topology and cache reuse. Foreground per-phase read work under one treatment discriminates; no cache or codec change is justified solely by these counters. |

## Final report review

The final findings draft was read in full. The recommendation is exactly one separately authorized unchanged-policy, byte-weighted coverage diagnostic, not a matcher, codec, anchor, canonical or placement optimization. Its one variable, same157 workload/oracle/environment, explicit terminal population, snapshot protocol, timing/allocation boundaries, stop rules and negative-result retention address the present decision gap. Keeping #87 open is correct because useful missed-byte opportunity has not been measured.

Independent CSV aggregation (no Store reopening or group re-decoding) confirms selected role totals366141 objects/799525289 canonical B and all selected objects in L. Payload86417 objects/705162954 canonical B has maximum canonical length32789 B, so the entire final payload set is eligible by size. Its count equals86417 measured eligible events, and source has no observed memory-policy exclusions. These facts support run-specific equivalence of total payload to unique eligible targets, but do not supply the missing terminal byte split. Retain the direct initially-missing byte counter as unavailable; present any total equivalence as an explicit source-supported derivation, never as directly measured instrumentation.

The independently summed C/D CSV equations are: encoded group bytes295240878; decoded group bytes715758049 =156296 group-count bytes +1464564 record-directory bytes +693285484 FULL record bytes +20851705 DELTA record bytes. Pack BLOB295944702 =78640 pack-header bytes +625184 pack-directory bytes +295240878 encoded-group bytes. FULL693285484 =358117 kind bytes +692927367 canonical bytes. DELTA20851705 =328984 header bytes +3676626 COPY instruction bytes +1769685 INSERT framing bytes +15076410 literal bytes. Reconstructed DELTA target canonical bytes belong to logical role totals and are not added to record bytes. Homogeneous role encoded bytes and exact decoded mixed composition remain separate.

Review corrections were communicated before sealing: physical derived-field statuses and nested BLOB populations; cgroup converted/delta field status and boolean/identity units; missing group offset/selected-unselected/admission-provenance columns; explicit first-span defect distinction; lifecycle elapsed provenance; and run-specific eligible-total inference versus unmeasured outcome bytes. None requires another census or product execution. The final report must retain those corrections and the exact null reasons. The regenerated counter reconciliation includes ready/closed elapsed maps and resolves the lifecycle provenance comment. No fundamental conservation, custody or target-selection objection remains; final artifact hashing/publication is the parent owner’s remaining responsibility.

