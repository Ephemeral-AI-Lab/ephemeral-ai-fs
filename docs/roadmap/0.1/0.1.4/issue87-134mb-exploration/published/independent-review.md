# Independent final review of the134.2 MB architecture findings

Reviewed the complete `findings.md` against the shared sealed accounting, compression/prefix subreports and source mechanisms. This was a bounded source/report review; no original Store access, duplicate inventory, retained-data encoding or replay was performed.

**Conclusion: the architectural direction and target budget are defensible as an experiment, not an expected result.** The report explicitly retracts the unsupported10–20% range, distinguishes exact observations from hypothetical budgets, and does not promise60% savings.

Checks completed:

- **Target arithmetic:**335552512−134221004=201331508 B. The illustrative70000000 payload +45000000 structure +19000000 other budget totals134000000 B, leaving221004 B beneath the target. It is correctly labelled a required future budget rather than an observation. All current index/page/allocation costs must fit the future “other” allowance;18.8MB of current index pages alone does not establish a19MB complete overhead result.
- **Nonoverlapping byte populations:**216448341 payload-group B +78792537 structural-group B +703824 pack-framing B =295944702 pack BLOB B. “Mixed-role” is correctly limited to structural mixtures after the exact role join. Canonical input sizes and reconstructed DELTA targets are not added to physical frame bytes. Inode-table and wrapper group envelopes are explicitly shared upper bounds, not exclusive savings.
- **Git qualification:**35244143+11738390=46982533 blob-entry B;3138693+1837582=4976275 tree-entry B; those totals plus31060 commit-entry B and32 outer B equal51989900 B. The223334694 B FULL-to-delta difference uses the same110081-object membership. The report preserves different metadata/packing costs and maximum depths, and states that a final Git pack may use bases unavailable to a chronological LayerFS writer. It does not mistake the final pack for an online prior-only result.
- **Whole-file tradeoff:**795755677+96137390=891893067 unique regular-file payload B;75736+186=75922 unique regular-file contents. Comparing891893067 with703348161 current unique file-chunk payload B gives188544906 B of lost raw chunk sharing. The comparison is correctly raw, not allocated. It must be repaid by history encoding, not subtracted from a compressed total.
- **Exact reconstruction bounds:** the proposed262144-byte threshold is explicitly raw file payload. Prefix compression operates on that payload, while canonical framing is separately authenticated and charged. The1048576-byte closure cap includes base, intermediate versions and target; four262144-byte decoded nodes allow only three delta edges at maximum size, despite the independent four-edge limit. This avoids the framing/window off-by-one ambiguity raised during subagent review. Actual prefix/window/buffer parameters still need freezing before encoding.
- **Structural lineage:** preserving canonical inode-leaf bytes and IDs while passing a tree editor's old origin as an optional physical predecessor is a coherent physical-only alternative. It requires selected-base existence/authentication, resource accounting and FULL fallback where origin is absent or inadmissible. It does not justify a free similarity index or prove a compression ratio. Canonical file-root changes alter inode bytes and group membership, so payload and structural effects are correctly marked nonadditive until a complete combined image is measured.
- **Experimental validity:** the chosen first screen is bounded whole-file actual-prior prefix representation, not a level/window sweep. Attributing a benefit specifically to history encoding requires a FULL alternative with the same canonical unit and codec settings; any comparison with the current complete representation must additionally account for changed wrappers, lost chunk sharing, regenerated structure, bases and index/container bytes. A payload-only image is explicitly not a134MB Store. The report retains the need for fresh synchronous public-path evidence after offline potential is measured.

One factual correction was requested during review: current M4 reads are not always “one≤64KiB group.” DELTA reconstruction can require both a target group and its separate selected FULL-base group, plus metadata traversal; all8024 observed DELTAs cross groups. The comparison should say that current reads use≤64KiB group units and DELTAs may additionally require a FULL-base group. This correction does not change the conclusion that the proposed whole-file/chain design can substantially increase cold small-range work.

No other factual blocker was found. The quantities70000000/45000000/19000000 remain hypotheses to test; they are not inferred from Git's metadata semantics or a codec benchmark. The report must retain negative results and reject a candidate that cannot fit complete allocation/read-cost accounting rather than retroactively changing the target population.
