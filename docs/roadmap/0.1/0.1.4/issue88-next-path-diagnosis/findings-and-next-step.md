# Next feasible path: reconciled independent diagnosis

**ADDITIONAL DIAGNOSTIC REQUIRED.** Three independent source/evidence reviews agree: the immediate next experiment is the unchanged-policy, byte-weighted file-payload correspondence/handoff diagnostic on accepted M4.5. Native prior-version compression remains the eventual optimization direction. Neither a codec rewrite nor a budget increase is selected for execution by this report.

## Scope and confidence

Reviewed issue87/88 contracts, existing aggregate results, source checkout 6f1c120c68f1532a88eb866c2af593b74c3cc772 and the prior diagnostic design. No Store was opened, and no census, encoding, build or replay was run. Sources and existing aggregate receipts are hashed in identity-and-scope.json and evidence-index.json. The three independent reports are physical.md, coverage.md and review.md.

The issue88 full157 receipts report performance, verification and cleanup PASS, with final acknowledgement allocation 302,006,272 bytes. This review does not certify a completed sealed full-run report: its existing findings still have a full-history placeholder and final census/report summaries remain unfinished. Preserve that distinction.

## Reconciled findings

| Finding | Evidence | Claim status |
|---|---|---|
| Public coverage is a substantial unresolved bottleneck | Hints computed before CAS; each metadata grant reserves 131,136 bytes, permitting 127 operation grants under 16 MiB. All 152 affected original Commits saturate 16,654,272 reserved bytes. | Source-supported mechanism; useful missed-byte savings unknown |
| No-hint cannot simply mean new content | Prior oracle-only analysis bounds completed no-overlap at 65,446,350 canonical bytes and leaves at least 240,892,334 canonical bytes needing another explanation, under its span/same-path premises. | Derived conditional bound, not recoverable compressed bytes |
| Offline native-prefix candidates are not proven available to the public encoder | Offline first overlap uses complete source history after global CAS; public hints are bounded, before CAS, and depend on first surviving producer occurrence. | Proven source difference |
| S2 is not a codec-only treatment | Native level3/per-unit frames, selected-prior reconstruction through four edges and offline correspondence differ from group-level encoding, custom programs and FULL-base policy. | Proven source/contract difference |
| Generic missing-span diagnosis is incorrect on S1 | Inode-leaf origin hints intentionally have no file span; S1 eligibility includes both leaves and chunks. Metadata-value chunks also lack required file-content provenance. | Proven source finding |
| Reused-triggered grants are not necessarily wasted | A reused chunk can initialize a cursor needed by a later missing chunk. | Source-supported and prior synthetic witness; avoidable fraction unknown |

The original public path's 8,468 hinted payload targets and S2's 58,323 prefix trials describe different populations. They identify a delivery question, not a measurable coverage percentage or savings forecast.

## What the byte budget permits us to claim

All values below are integer bytes; rounded MB presentation is decimal. Allocated and logical sizes refer to acknowledgement receipts; offline figures refer to experimental containers/groups.

- Original allocation 335,552,512; S1 allocation 302,006,272; observed difference 33,546,240.
- Logical database 318,832,640 to 296,845,312; difference 21,987,328.
- Signed allocation adjustment 16,719,872 to 5,160,960; difference 11,558,912. Its cause remains unresolved; do not attribute it to encoded structural savings.
- S2 matched FULL container 243,582,540; selected container 102,306,097. This is offline content potential, excluding production structures/index/pages and five metadata-value chunks.
- S3 selected container 99,824,523, only 2,481,574 below S2. Canonical migration is not justified as the next commitment.
- Hypothetical S2 frames plus offline S1 structural groups: 102,306,097 + 56,857,102 = 159,163,199, already 24,942,195 above the 134,221,004 target before remaining costs. This is a scenario arithmetic check, not a measured combined Store or a theoretical lower bound.

The aggressive target remains 134,221,004 allocated bytes. No expected achieved ratio can responsibly be assigned to the next diagnostic, which changes observations rather than storage policy.

## Exactly one next experiment

**Implement, review and separately authorize the unchanged-policy payload correspondence/handoff diagnostic against accepted M4.5 source eb7050603c5ee97a02dfa0d5357619079d25ab51 and its authenticated measured product identity.** Use an isolated branch; do not reset the existing experimental checkout. The reporting revision alone is not the measured binary identity. Preserve original baselines and evidence.

Hypothesis: operation allowance or incomplete handoff explains a substantial byte-bearing part of the predecessor-present/no-hint cohort; completed legitimate no-overlap does not explain it all. This is a coverage hypothesis, not a claim of useful deltas or compressed savings.

One variable: diagnostic observations enabled. Control/candidate retain identical candidate order, CAS probes, cursor decisions, budgets, canonical encodings, codec, grouping, FULL-base policy and public route. Do not add early CAS probes, refunds, retries, hint merging, new predecessor searches or prefix encoding.

Reuse the existing reviewed two-byte reason/grant design and aggregate validator. Exact file-content role/source filtering is mandatory, including separating metadata values. Count unique initially missing eligible file payloads and full canonical bytes at the authoritative CAS owner, before optional matching-memory exclusion. Keep occurrence work before CAS and overlapping trials separate.

Exclusive cursor categories: no predecessor; missing required span; complete empty; limited empty; complete with hints; limited with hints. Record memory/file/operation/descriptor first-denial and inherited reasons, then preserve actual base, search, group selection and admission outcomes. Partial hints can still produce admitted DELTAs. Attribute grants triggered by reused/duplicate/missing occurrences once, without labelling them avoidable work.

Minimal validity gate: prove single ownership, exact role/transport identity, authoritative successful pack/location provenance and the complete 158-row Init+157 cohort chain. Require eligible attempt/new-selected counts AND bytes to reconcile, including DELTA count/bytes and late-race handling. Equality of totals alone is insufficient. If ownership or race checks fail, retain attempt evidence and mark unique attribution unavailable; do not fabricate a complete funnel.

Freeze the same157 workload/oracles, macOS host Store/coordinator and existing Docker execution topology. Preserve original acknowledgement allocation; separate public, transfer, preparation, observer, case and enclosing times. Keep source/binary/image seals strict. Record one final pre-verification logical snapshot under separate custody, with detailed census only at Init/final as in the existing prospective contract. No per-Commit Store copies or graph traversals. Copy allocation cannot substitute for original allocation; disclose observer/cache effects.

Stop and preserve partial/negative results on policy/layout/spill-capacity changes, missing causal state, role ambiguity, ownership/race invalidity, identity/oracle/cleanup/resource failure. Existing time/resource limits remain; do not invent a storage gate. The experiment is informative if it identifies the bytes blocked by each actual mechanism, or falsifies the suspected mechanism. No replay is executed by this review.

## Decision after the diagnostic

If a specific byte-bearing coverage loss dominates, test only that identified mechanism next. If coverage is adequate, freeze a native-prefix encoding experiment on the actual available-base population. This conditional decision rule does not schedule a second experiment now.

The later smallest native-prefix boundary preserves canonical chunks/CDC but needs an explicit physical record/version, authenticated bounded base closure and writer/read-budget integration. Current DELTA records are COPY/INSERT programs and current reads require FULL bases. Four edges on maximum32KiB chunks imply at most163,840 raw closure bytes; the offline1MiB cap is not the active limit. Public accounting must additionally include enclosing group/page reads, codec contexts, pending buffers and read amplification. One fixed offline read rose from68,000 to340,125ns; fixed probes do not establish latency percentiles. The owner’s roughly30% foreground tradeoff applies to meaningful eventual storage gains, not to claiming diagnostic success.

No whole-file rewrite, deeper-chain policy, global similarity search, backend change or rollout is selected. Keep #87/#88 open for the stated diagnostic and unresolved complete-Store objective. Completion of this bounded review does not complete the earlier full-run report or qualify a release.
