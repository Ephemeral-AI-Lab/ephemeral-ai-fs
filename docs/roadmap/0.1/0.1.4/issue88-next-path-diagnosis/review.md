# Independent next-path review

Scope: existing issue88 contracts, findings, review, source and JSON receipts in `/Users/yifanxu/Ephemeral-AI-Lab/layerfs-issue88-experiments` and sibling runs. No Store opened, census run, build, encoding, benchmark or historical artifact modification. Only this temporary report is written.

## Current claim strengths and missing finalization

The original acknowledgement receipt335552512 B and new public S1 acknowledgement302006272 B support an observed33546240 B allocation reduction, about10%. But logical database length falls318832640→296845312 B, only21987328 B. Signed allocation adjustment falls16719872→5160960 B, accounting for11558912 B of the observed allocation difference. The adjustment is not measured reclaimable encoded space and cannot be forecast as recurring. This exact arithmetic prevents calling the whole33.55MB delta structural compression.

New full157 performance and verification summaries/results report PASS, cleanup PASS,157 records and4936693030 independently verified bytes. Candidate canonical totals799576457 B/366293 objects differ from historical799525289 B/366141 objects by51168 B/152 objects; equal filesystem states do not imply identical internal populations. It is a historical allocation comparison, not a fresh paired timing result.

At this review, `issue88-experiments/findings.md` still contains `<!-- FULL_HISTORY_RESULT -->`; `issue88-report-1` contains offline-results/public-smokes but no completed public-full or census summary; `issue88-s1-final-census-1` contains only roles-inventory.sqlite. Source `census_report.py` and `summarize_full.py` exist but are untracked and their presence does not prove execution. Therefore final nested accounting/sealing/publication remains unfinished. Do not describe a completed sealed final diagnosis until actual output manifests and summary equations have been reviewed. This does not erase the valid acknowledgement/verification receipts.

Sources: `issue88-s1-full157-1/deepseek-full/performance-result.json`, its verification result and both phase summaries; original full157 final receipt; current findings/census/report directories.

## Native-prefix evidence and the134MB target

S2 selected framed content102306097 B is a real offline result on86412 regular-file chunks/703348161 raw B; its matched same-unit FULL container243582540 B isolates a large potential benefit under the frozen native-prefix policy. It excludes five metadata-value chunks and all production structural/index/SQLite allocation. Its first-overlapping same-path predecessor comes from the complete authenticated extractor, not the product's currently bounded hint delivery. It reconstructs the actual selected prior through up to4 edges, rather than substituting a depth-one FULL anchor. Zstd3/window20 and independent per-unit framing also differ from current group encoding. It is not a codec-only treatment relative to production.

S3 selected container99824523 B improves S2 by only2481574 B, at the cost of130552367 additional raw unit bytes and a new canonical unit. Unknown wrapper/index savings cannot rescue a promised134MB result; prior advice to prioritize the whole-file rewrite must remain revised.

Even the deliberately hypothetical sum S2 content102306097 + offline S1 structural groups56857102 =159163199 B is24942195 B above134221004 before remaining framing/index/metadata/allocation. This is not a measured combined image: group membership, base retention, public budgets and framing differ. The arithmetic shows that neither simply stacking these results nor deleting a few wrappers establishes the target. Using the new actual S1 Store302006272 as if S2's FULL-to-prefix141276443 B difference were directly subtractable would be invalid for the same reasons.

Every S2 physical base is a selected earlier target and was retained in the offline image; no base-only omission was found. That fact does not guarantee the same graph/closure under product candidate availability or a later new writer. Chain reads are substantial: the matched follow-up's representative S2 maximum-closure small read median68000→340125ns is approximately5×; S3 maximum closure384209→1058625ns approximately2.76×. These are three fixed alternating pairs on selected representatives, not distributions or OS-cold measurements. They suffice to reject “bounded bytes means cheap reads,” not to establish public latency.

Sources: `issue88-s2-b9abb13-1/result.json`, `issue88-s3-b9abb13-1/result.json`, `issue88-experiments/read-cost-contract.md`, latest findings/read-cost summary. Earlier independent review authenticated all target offsets/chronological priors/selected margins and artifact hashes; no duplicate decoding was run here.

## Exactly one immediate next step

**Run the separately authorized, unchanged-policy, chunk-specific handoff/coverage diagnostic before a native-prefix public prototype.** Eventual production direction remains canonical-preserving native-prefix encoding, but it is not yet an attributable one-variable public experiment. Implementing new record kind, recursive base reads and a new predecessor discovery path simultaneously would conflate coverage, matching and dependency policy.

Freeze the diagnostic against one exact product baseline and preserve its codec, candidate hints, limits, grouping and wire format. If using public S1 as baseline, record S1's structural search explicitly and partition by exact role. Scope file-content payload targets after authoritative initial CAS filtering; preserve counts AND canonical bytes. Carry observed predecessor, first-span and actual cursor completion/stop reasons to the existing candidate owner, with unique-cohort validation or explicit race-invalid attempt evidence. A leaf's physical origin intentionally has no file span: **never classify structural inode leaves as missing-span defects** merely because `eligible_targets` now combines leaves and chunks. Metadata-value chunks also need separate use/source qualification.

Report the unique chunk funnel's no predecessor, missing required span, completed no overlap, incomplete memory/file/operation/descriptor allowance, prior/base unavailable, search limit, candidate/group rejection and admitted representation. Partial hints and incomplete extra trials must not erase a winning candidate. Pre-CAS occurrence work and overlapping trial events remain separate from unique target bytes. Track grant expenditure triggered by reused/duplicate/missing occurrences without calling it avoidable waste.

Use fixed counters/histograms plus successful admission-transaction provenance, final one-pass metadata union and source-reviewed prefix/ack equality gates. No full traversal/copy after each Commit, per-target timed logs, additional prefix compression or new candidate searches in this diagnostic. Record observer/cache effects and primary acknowledgement allocation. Stop/preserve on missing causal state, role ambiguity, race-bijection failure or correctness/custody/resource failure. The output must tell which byte-bearing coverage loss can actually be addressed by one later declared change.

After that diagnostic, freeze an encoding prototype on the same authenticated available-base population to test native prefix and explicit depth/byte policy. This is a later experiment, not a second action bundled into the immediate recommendation. If coverage is already adequate and native encoding remains the dominant opportunity, the diagnostic can justify proceeding without inventing a coverage optimization. If coverage blocks the large population, fix or test that single source mechanism first. Neither outcome promises134MB.
