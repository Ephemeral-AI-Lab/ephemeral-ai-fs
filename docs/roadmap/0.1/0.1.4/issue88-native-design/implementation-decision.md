# P implementation decision after C

C all157 performance/verification/cleanup and authenticated158-row cohort gate
PASS. Shared census authenticates366280 selected objects, all in retained graph
union, no physical-base-only/unselected residue. Acknowledged allocation234897408B;
pack BLOB204745502B; payload groups125298018B, structure78743788B. This is C alone.
Retain C only as research foundation: historical Commit42.826→73.714s (+72.12%)
and individual second-scale regressions are retained. No production acceptance.

Proceed with the existing P contract on C plus fixed S1. S1 port is isolated in
combined-control and will be committed/imported before P's first compiled proof.
No candidate samples until both exact sources, mandatory tests, host/image and
frozen smoke command/ordering identities are recorded. Both arms share C+S1;
new P counters will be projected by the same harness in both arms.

Adopt the reviewed version2 direct-record extraction as part of the prospective
native reader, before implementation/samples. Amend section5 read charges to actual
requested32-byte outer framing plus4-byte count, complete4*N end-directory and
selected record bytes. Charge parsed directory/record bytes plus reconstructed
canonical bytes to decoded work; unrequested neighboring records are not charged
as reads. All existing numeric ceilings remain. Every extra range or duplicate
extraction is charged; no refunds/retry with reset counters. Version1 unchanged.

Amend section2/4 fixed-header validation scope: point reads validate the complete
native end-directory and selected fixed header/frame/dependencies, not unrelated
record bodies. Whole-group/census validation validates all fixed headers and every
reconstruction. This is an explicit point-read integrity scope; no claim of full
pack/directory/body authentication from partial I/O. Reuse pack offset validation.
The attached point-read review defines exact five-range order, connection release,
capacity and corruption checks. No cache, global index, additional format field,
parameter adjustment, deeper chain or new allowance.

One coherent P physical replacement: first delivered prior, staticZstd1.5.7L3,
window20, depth4, RAW native groups, exact5/37Bheaders, fixed fallback/owner rules.
Payload codec can accept or lose; do not extrapolate old S2 output or add S1 savings.
Fresh matched smokes precede explicit full157 promotion. Stop on hard correctness,
identity/closure/resource/cleanup failure; preserve negative results. No migration,
rollout, benchmark definition/fixture change, M5/cloud work or merge.
