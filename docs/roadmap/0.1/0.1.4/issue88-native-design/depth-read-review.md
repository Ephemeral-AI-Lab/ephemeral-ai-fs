# Independent depth-stratified read diagnostic review

Status: authorized finalization task, source/proof review in progress; no new read result is certified yet. The earlier184,598,528-byte full157 experiment remains complete and sealed. Reviewer owns this report and `test-debt-ledger.md` only. No build, test, Store/inventory scan, copy or measurement is performed by this reviewer; the coordinator serializes all resource-consuming work.

## Prospective proof requirements

The frozen selected range must be4096 bytes fully contained in the authenticated target extent. Prove retained namespace root→byte-safe path→inode/FileState→extent logical/source offsets→selected canonical target identity separately for each arm, with matching source/oracle bytes. Historical cache or source-blob membership is a shortlist, not that proof. For the candidate, prove native representation, selected locator, exact transitive dependency chain and chronology at the chosen retained checkpoint. Use existing decoders and bounded selected-path work; no replacement census.

Static target proof and dynamic aggregate counters serve different purposes. A depth-bin lower bound or extra native fetch may come from unrelated read-ahead. It cannot uniquely identify the requested target. Combine the contained target/path proof with source-supported required-read behavior, a fresh process/FUSE context with no target pre-read, exact count/digest and expected minimum native decode/fetch/dependency activity. Depth0 requires a native FULL target plus actual native work, not merely a depth0 histogram event that can also accompany a legacy FULL optional prior. Full-file reads are mixed-depth workloads chosen by a target’s stratum.

Adapted probe binaries must be separately sealed against unchanged frozen product sources and matching pinned dependencies. Their hashes are new; existing normal producer host/image identities remain references, never substituted for new probe identity. Time public Exec through terminal output drain; digest and observers remain outside that timer. End-only FUSE receipt scope and unavailable phase/write counters remain explicit. Preserve all60 fixed rows or exact stopped/invalid state, no replacements, and clean every owned resource.

The test debt ledger is complete as documentation, with all nine failures still unresolved. Final disposition awaits actual reviewed cohort/probe sources, frozen execution evidence and the independent results review. Planning text in the historical plan is superseded by the user’s current execution authorization, not retroactively edited into the old contract.

## Selected-path preparation source review

Pre-execution source status: **clear to attempt the one bounded preparation under the coordinator’s resource supervisor**. No selected cohort or public result is asserted yet. Reviewed `depth-read/select_cohort.py`, `proof/src/main.rs` and the new authorized `depth-read-contract-v1.md`.

The Rust helper uses the existing product ObjectSource reader, `filesystem::stat` and `rope::visit_extents`; it resolves the specified typed Commit root and byte-safe path, requires a regular file, matches declared file length, and proves the4096-byte request lies wholly in the exact target’s ordered extent. Canonical structural reads authenticate ID and length with fixed4096-read/16MiB-returned-byte/time/descriptor bounds. It opens only coordinator-supplied independent disposable copies and does not digest/read payload through a private timing shortcut. The parent’s first compile type correction changes a SQL u64 extraction to checked i64→u64; no policy change was introduced. Parent reports four focused proof checks passing; reviewer ran none.

The selector uses the sealed older source-chunk cache as proposals, then requires both retained-path proofs, paired source/oracle identity, canonical metadata equality, source full/range digests and a native selected chain with strictly decreasing pack IDs. The first proposed matching candidate is not silently replaced if proof fails. Its trajectory is now bound to the completed sealed report and matching validator performance/inventory/snapshot hashes, with exact0..157 row order. Cheap deadline checks now cover inner path/chunk search loops. Both corrections were required by this review before execution. Copy/input hashes are rechecked after proof; representation/extent composition is metadata only. Actual RSS/free-space observation belongs to the coordinator’s declared bounded supervisor, not to an inference from source allocation counts.

## Public probe source review — fixes requested before campaign

The adapted literal byte-path OsString argument reaches Bash `"$1"`; the existing daemon preserves raw argv bytes. The60-row schedule, public Exec→terminal timer, separate digest, End-only FUSE scope and owned-container removal remain appropriate. Review requested these remaining small preflight bindings from the probe owner:

- Bind the governing authorized depth-read amendment as well as the older published reference contract; do not identify the new campaign solely with the old contract hash.
- Reject inode/device aliasing of disposable copies to immutable snapshots/originals and bind the exact copies used by the selected-path proof to those used by the campaign.
- Cross-check candidate span/locator/raw-length/closure fields against the sealed proof body/chain, not just its smaller selection-identity dictionary.
- Record setup native activity and require no setup native pre-read before candidate timing; require native fetch/decode/edge/depth lower bounds for the selected all-native chain. These counters corroborate static target proof and required-read semantics, not an ObjectId trace.

Preparation may proceed independently of these probe-only fixes. No timed observation or source-level declaration of campaign PASS is allowed before they are reconciled and actual adapted binaries/cohort/schedule are sealed.

## Final pre-build probe source reconciliation

**No unresolved blocking source finding remains before the coordinator’s build/check gates.** The probe now binds the governing depth-read amendment and the preserved earlier reference contract; checks exact cohort/proof/campaign copy identity and rejects hardlink aliases to immutable snapshots/originals; compares both arm path proofs and candidate selected-span/chain-head/locator/raw/depth/closure fields; and verifies actual setup native fetch/decode counters are zero before read timing. Timed candidate reads require the declared depth bin plus at least d+1 native fetches/decodes and d dependency edges for the selected all-native chain. Those are corroboration of the static contained-range proof, not a per-ObjectId attribution from counters alone.

The source retains literal raw path arguments through SDK/daemon/Bash, fixed60-row order, separate digest, unavailable per-phase FUSE/write fields, actual End receipt presence, resource/deadline checks and independent owned-container removal. No product instrumentation or encoding change was added. The coordinator’s preparation supervisor labels observed process-tree RSS and child lifetime high-water separately and enforces finite elapsed/free-space bounds; it does not claim an exact simultaneous peak between samples.

This clears the reviewed source for bounded execution gates under the user’s finalization authorization. Actual proof selection, adapted binary identities, tool checks and campaign results remain pending. Reviewer executed none of them and changed only the two assigned review documents.
