# Independent depth-stratified read diagnostic review

FINAL STATUS: all60 fixed read observations and their count/digest/dependency/resource/cleanup checks reviewed PASS. Retain the isolated research checkpoint with the selected-case limitations below; research closure is justified after additive report sealing/publication. Earlier pending notes are historical review stages superseded by the final disposition. The earlier184,598,528-byte full157 experiment remains complete and sealed. Reviewer owns this report and `test-debt-ledger.md` only. No build, test, Store/inventory scan, copy or measurement is performed by this reviewer; the coordinator serializes all resource-consuming work.

## Prospective proof requirements

The frozen selected range must be4096 bytes fully contained in the authenticated target extent. Prove retained namespace root→byte-safe path→inode/FileState→extent logical/source offsets→selected canonical target identity separately for each arm, with matching source/oracle bytes. Historical cache or source-blob membership is a shortlist, not that proof. For the candidate, prove native representation, selected locator, exact transitive dependency chain and chronology at the chosen retained checkpoint. Use existing decoders and bounded selected-path work; no replacement census.

Static target proof and dynamic aggregate counters serve different purposes. A depth-bin lower bound or extra native fetch may come from unrelated read-ahead. It cannot uniquely identify the requested target. Combine the contained target/path proof with source-supported required-read behavior, a fresh process/FUSE context with no target pre-read, exact count/digest and expected minimum native decode/fetch/dependency activity. Depth0 requires a native FULL target plus actual native work, not merely a depth0 histogram event that can also accompany a legacy FULL optional prior. Full-file reads are mixed-depth workloads chosen by a target’s stratum.

Adapted probe binaries must be separately sealed against unchanged frozen product sources and matching pinned dependencies. Their hashes are new; existing normal producer host/image identities remain references, never substituted for new probe identity. Time public Exec through terminal output drain; digest and observers remain outside that timer. End-only FUSE receipt scope and unavailable phase/write counters remain explicit. Preserve all60 fixed rows or exact stopped/invalid state, no replacements, and clean every owned resource.

The test debt ledger is complete as documentation, with all nine failures still unresolved. Final disposition awaits actual reviewed cohort/probe sources, frozen execution evidence and the independent results review. Planning text in the historical plan is superseded by the user’s current execution authorization, not retroactively edited into the old contract.

## Selected-path preparation source review

Pre-execution source status: **clear to attempt the one bounded preparation under the coordinator’s resource supervisor**. No selected cohort or public result is asserted yet. Reviewed `depth-read/select_cohort.py`, `proof/src/main.rs` and the new authorized `depth-read-contract-v1.md`.

The Rust helper uses the existing product ObjectSource reader, `filesystem::stat` and `rope::visit_extents`; it resolves the specified typed Commit root and byte-safe path, requires a regular file, matches declared file length, and proves the4096-byte request lies wholly in the exact target’s ordered extent. Canonical structural reads authenticate ID and length with fixed4096-read/16MiB-returned-byte/time/descriptor bounds. It opens only coordinator-supplied independent disposable copies and does not digest/read payload through a private timing shortcut. The parent’s first compile type correction changes a SQL u64 extraction to checked i64→u64; no policy change was introduced. Parent reports four focused proof checks passing; reviewer ran none.

The selector uses the sealed older source-chunk cache as proposals, then requires both retained-path proofs, paired source/oracle identity, canonical metadata equality, source full/range digests and a native selected chain with strictly decreasing pack IDs. The first proposed matching candidate is not silently replaced if proof fails. Its trajectory is now bound to the completed sealed report and matching validator performance/inventory/snapshot hashes, with exact0..157 row order. Cheap deadline checks now cover inner path/chunk search loops. Both corrections were required by this review before execution. Copy/input hashes are rechecked after proof; representation/extent composition is metadata only. Actual RSS/free-space observation belongs to the coordinator’s declared bounded supervisor, not to an inference from source allocation counts.

## Public probe source review — fixes requested before campaign

The adapted literal byte-path OsString argument reaches Bash `"$1"`; the existing daemon preserves raw argv bytes. The60-row schedule, public Exec→terminal timer, separate digest, End-only FUSE scope and owned-container removal remain appropriate. Review requested these remaining small preflight bindings from the probe owner:

- Bind the governing authorized depth-read amendment as well as the older published reference contract; do not identify the new campaign solely with the old contract hash.
- Reject inode/device aliasing of disposable copies to immutable snapshots/originals and bind the exact copies used by the selected-path proof to those used by the campaign.
- Cross-check candidate span/locator/raw-length/closure fields against the sealed proof body/chain, not just its smaller selection-identity dictionary.
- Record setup native activity and require no setup native pre-read before candidate timing; require native fetch/decode/edge/depth lower bounds for the selected all-native chain. These counters corroborate static target proof and required-read semantics, not an ObjectId trace.

Preparation may proceed independently of these probe-only fixes. No timed observation or source-level declaration of campaign PASS is allowed before they are reconciled and actual adapted binaries/cohort/schedule are sealed.

## Final pre-build probe source reconciliation

**No unresolved blocking source finding remains before the coordinator’s build/check gates.** The probe now binds the governing depth-read amendment and the preserved earlier reference contract; checks exact cohort/proof/campaign copy identity and rejects hardlink aliases to immutable snapshots/originals; compares both arm path proofs and candidate selected-span/chain-head/locator/raw/depth/closure fields; and verifies actual setup native fetch/decode counters are zero before read timing. Timed candidate reads require the declared depth bin plus at least d+1 native fetches/decodes and d dependency edges for the selected all-native chain. Those are corroboration of the static contained-range proof, not a per-ObjectId attribution from counters alone.

The source retains literal raw path arguments through SDK/daemon/Bash, fixed60-row order, separate digest, unavailable per-phase FUSE/write fields, actual End receipt presence, resource/deadline checks and independent owned-container removal. No product instrumentation or encoding change was added. The coordinator’s preparation supervisor labels observed process-tree RSS and child lifetime high-water separately and enforces finite elapsed/free-space bounds; it does not claim an exact simultaneous peak between samples.

This clears the reviewed source for bounded execution gates under the user’s finalization authorization. Actual proof selection, adapted binary identities, tool checks and campaign results remain pending. Reviewer executed none of them and changed only the two assigned review documents.

## Actual frozen cohort and build-identity review

The selected cohort reports PASS after five candidate-path attempts,54,910 source digest bytes and10,957,560,292 ns selection observer time. Reviewed the five existing small proof JSONs and frozen campaign/build identity JSONs only; no Store/inventory access or hashes during the running campaign. Static control/candidate FileState IDs, metadata roots and selected spans agree for each case; dependency-chain lengths/raw sums and exact proof copies agree with the cohort. The frozen schedule has60 rows. Both probe identity sidecars and the frozen schedule contain the same six-file tool map, while their product/source seals match their respective original normal producers. Actual probe hashes are distinct new identities: control341a2ff2fefdf1a491d2b55ccc63f07eaa0c56cc6d527cfa6ccb461ad067a608; candidate0c1ad49a3df6992e8327c936aafaa00193c2256191fbfdcb980190613905f14b. Identity values were compared as recorded receipts, not independently rehashed here.

|Selected depth|Source checkpoint|File bytes|Selected raw closure bytes|Actual containing-file payload composition|
|---:|---:|---:|---:|---|
|0|1|17,819|17,819|One native FULL extent|
|1|2|9,128|12,609|One native PREFIX depth1 extent|
|2|3|11,235|23,844|One native PREFIX depth2 extent|
|3|4|12,408|36,252|One native PREFIX depth3 extent|
|4|5|4,320|17,392|One native PREFIX depth4 extent|

Every selected offset is0 and every4096-byte range is wholly contained in that one extent with source offset0. Depth0 is AGENTS.md; depths1–3 are successive early versions of the same `.agents/skills/dsh-code-review/SKILL.md`; depth4 is `.github/workflows/ci.yml`. The maximum actual closure is36,252 B at depth3, far below the retained format’s163,840-B possible closure. Depth4 has more edges but fewer reconstructed raw bytes than depth3. This predeclared deterministic selection is valid and must not be changed after seeing timings.

The final report must describe this actual population: five early-history file occurrences, three distinct paths,4,320–17,819-B files. It is not an8MiB full-file sample, later-history/locality sample or maximum-byte-closure stress case. Although the contract permits mixed-depth containing files, these five files each contain one target payload extent; do not claim actual mixed-depth coverage. Both range and full reads demand the same whole canonical target/chain, so their comparison is not a general streaming-throughput test. Three repeats per arm/cell do not establish a population tail or a causal depth-only slope.

The running campaign remains unreviewed as a complete result. No interim favorable sample is used for selection or disposition. Final source/copy seals, all row/cleanup outcomes, native setup/demand evidence and read-cost interpretation will be reviewed after the coordinator reports completion.

## FINAL campaign result and independent disposition

**RETAIN AS AN ISOLATED RESEARCH CHECKPOINT; close this research generation after final report sealing/publication under the user’s finalization authority.** All60 declared observations completed in the fixed order, with exact byte-count/digest, required native coverage, resource and cleanup checks PASS. No additional observation, encoder change, repair or optimization is requested. This closes the selected-case read question sufficiently for the research decision, not general read/adoption/release qualification.

Reviewed campaign summary, all60 existing result JSONs and recorded identity/source-seal maps, and source of the separate receipt-only `report.py`. No build, test, Store/inventory access, new measurement or large hash was performed by this reviewer. The campaign summary reports206,243,691,708 ns enclosing invocation,60 attempted/60 completed, no error and unchanged original trees. Recorded before/after original seal maps agree exactly in both arms. Each row has PASS/removed-container and successful Clean/head/no-Commit evidence; setup native fetch/decode counters are zero; count/digest executions have exit0 daemon receipts and no Docker Engine calls. Candidate read intervals satisfy depth-bin/fetch/decode/edge minima; control intervals report no native activity. Per-action FUSE counters correctly remain null, with present whole-session read receipts after End. All examined current/lifetime host and container memory, swap and elapsed checks remain within the frozen limits.

|Selected depth|Operation|Control median ns|Candidate median ns|Difference of medians ns|Relative difference|
|---:|---|---:|---:|---:|---:|
|0|range|8,110,750|8,597,792|+487,042|+6.005%|
|0|full|10,083,083|9,073,042|-1,010,041|-10.017%|
|1|range|12,635,709|11,473,375|-1,162,334|-9.199%|
|1|full|12,990,458|10,460,542|-2,529,916|-19.475%|
|2|range|10,718,792|11,432,166|+713,374|+6.655%|
|2|full|11,502,000|11,739,250|+237,250|+2.063%|
|3|range|13,619,667|13,080,625|-539,042|-3.958%|
|3|full|12,377,792|13,643,084|+1,265,292|+10.222%|
|4|range|12,493,292|10,920,958|-1,572,334|-12.585%|
|4|full|11,464,625|11,242,625|-222,000|-1.936%|

These medians are three repetitions per arm/cell, not population statistics. The row-by-repetition signed differences and all raw observations must remain in the final report; a difference of medians is not necessarily the median paired difference. The largest candidate observation is depth3/full repetition3:25,695,625 ns versus its paired control12,377,792 ns, +13,317,833 ns (+107.5946%). Keep it. Its host CPU is9,200,376 ns and native decode work504,623 ns; the execution receipt reports20,063,708 ns runtime and4,947,875 ns spawn. Those nested fields do not sum again into public time and do not establish the cause. It is neither discarded as noise nor a universal worst-case bound.

Median costs are mixed: changes range from−19.475% to+10.222%, with the largest positive median difference1,265,292 ns in depth3/full. This supports continued research retention alongside the already measured storage/construction benefit, while retaining the individual adverse observation. It does not overturn the previously observed all-history verification Exec+23.19% /CPU+26.17%, increased Commit write traffic or earlier74.42-ms read outlier. No new percentage gate or automatic30% waiver is used.

## Actual work versus selected-chain attribution

Timed candidate native decode calls are18/29/39/51/58 for strata0/1/2/3/4 respectively, identical between range and full operations within each stratum across these rows. Corresponding raw decoded work is41,501 /50,101 /75,488 /108,785 /115,653 B. These exceed the selected chain’s1–5 records and17,819 /12,609 /23,844 /36,252 /17,392-B raw closures. They are aggregate work inside the complete public path, including work beyond the specifically selected target. Do not divide them into target-only costs or attribute the extra work to read-ahead without source evidence. The contained static path/extent proof, zero native setup activity, fresh context, exact output and dynamic minima corroborate that the intended target was exercised; the counters do not identify every object accessed.

The actual cohort limitations remain decisive: five early checkpoints, three distinct paths, files4,320–17,819 B, one native extent per containing file, offset0 ranges, and a maximum selected closure36,252 B. The actual full-file cases are not mixed-depth payload files even though the contract permits them; dependencies themselves span earlier representations. This neither qualifies8MiB bulk files nor the163,840-B maximum closure, late-history locality, concurrency, old-writer/downgrade behavior or population tails. It is a finite descriptive check on exactly the selected retained cases. There is no causal depth-only inference from changing content/history/size across strata.

The receipt report source independently reconstructs the60-row order, verifies small input/report hashes, copies/identities/ready/input rows and resource/coverage outcomes, preserves signed n=3 statistics, and withholds comparisons on incomplete/invalid campaigns. Its declared container CPU window, End-only FUSE, unavailable container I/O and overlapping runtime scopes are appropriate. No blocking issue was found in that report source for the completed campaign; actual generated report reconciliation and final publication seals remain the coordinator’s final artifact steps.

## Research closure and adoption boundaries

The nine-entry `test-debt-ledger.md` remains entirely unresolved, with exact baseline comparisons, differing residues and the D opaque-result limitation intact. Closing this investigation does not mean those fault/boundary/schema/order/cache guarantees passed. No required legacy reader or S1 encoder is deleted just to claim simplification; one Store/CAS/index/publisher remains shared. Directory-Init provenance, old-writer open/write/downgrade protection, broad workload coverage and release/fault qualification remain explicit adoption decisions outside this task.

The authorized work now has a finite terminal outcome: completed184,598,528-B implementation experiment plus one successful bounded read diagnostic and an explicit research retain decision. Seal/push the additive finalization report and close #88 with these limitations; do not close related allocation/release issues or merge, migrate, deploy or start anchor-policy/SQLite/canonical tuning. No future diagnostic is introduced as a new closure prerequisite. All earlier pending-source, pending-cohort and pending-campaign notes in this document are resolved by the reviewed completed evidence and source reconciliations above.

## Final assembled-report check

Reviewed the executed receipt report (`issue88-depth-read-results-1/report.json`: PASS,60 validated rows, no errors) and final assembled findings (`issue88-depth-read-final-1/findings-and-disposition.md`). All ten read-cell min/median/max and signed median differences agree with the independently read raw results; paired differences remain separate. The preserved depth3/full unfavorable observation and aggregate18–58 native decode scope are correctly stated. Actual small single-extent early-history coverage is not exaggerated into8MiB, mixed-depth or maximum-closure qualification.

Recomputed the reported read-phase resource totals from the existing small JSONs: control/candidate host CPU176,440,544 /164,532,123 ns; maximum current and lifetime RSS41,926,656 /41,779,200 B (both metrics happen to have the same maxima here); host disk-read sums0 /0 B and writes122,880 /122,880 B. Maximum observed container lifetime memory across read/digest boundaries15,073,280 B and minimum recorded free space442,568,417,280 B match the findings. These remain scoped observations, not population estimates or physical no-I/O/cold-cache claims.

**Final review has no remaining blocking finding.** The results and documentation support the stated isolated-research RETAIN decision and authorized #88 research closure after sealing/pushing. The nine-failure ledger, previous verification/read regressions, old-writer/downgrade and Directory-Init limitations remain explicit. No new experiment, product edit, migration or merge is introduced. This review is ready to freeze with the final report; no additional measurements are needed for this disposition.
