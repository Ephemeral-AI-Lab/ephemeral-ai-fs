# Planning-only path to finalize issue88 read qualification

No experiment, selection scan, Store/inventory open, build, test or binary/hash execution was performed for this plan. Read source at native-payload HEAD0cdd2f353 and published/next-read-contract.md. The completed184598528B research checkpoint remains complete; this is a finite follow-on read-cost decision, not permission for storage tuning or product changes.

## Feasibility findings before promising reuse

1. The current standalone executable is NOT reusable unchanged. read-probe/src/main.rs117–128 accepts only sdk-text-32k/sdk-binary-8m, hardcodes32768/8388608 sizes and one computed range; its readiness record hardcodescheckpoint3 and dd always opens literal`file`. run.py fixes two smoke cases, state-3 fixtures,24rows and four copies. Reuse the SDK lifecycle/resource/receipt implementation, but build a new identical cohort-aware probe source against each frozen arm. Preserve old tool source, binaries,24-row results and seals. Existing producer binaries/images may remain unchanged and reused; a new probe binary is a separately authenticated observer/reader executable, not a new measured producer revision.

2. The final census inventory does NOT contain enough information for a one-SQL-query historical path/range selection. records provides canonical ID/role/selected locator; native_records provides actual prefix depth/raw lengths; edges only contains parent,child,use_label. Directory leaf names/InodeIds are omitted from reference edges, and extent leaf edges discard descriptor ordering, source_offset and logical_length. roots labels source tables, not complete source-checkpoint/path mapping. A path cannot be reconstructed from this adjacency alone. This is a concrete metadata limitation, not evidence that any depth stratum is empty.

3. A useful existing candidate aid exists: prior issue88 `extract_content.py` creates extraction.sqlite.blobs(id,path,sha,size,chunks), with ordered exact canonical chunk ID/offset/length/SHA per frozen Git blob. The source emits first-target JSONL too, but that is deduplicated and insufficient to enumerate every eligible occurrence in deterministic checkpoint/path/offset order. Prefer the sealed per-blob mapping plus existing source manifests; authenticate its original provenance and exact CDC/canonical compatibility first. Do not rerun a full source extraction or census just to recreate it.

4. Candidate generation is not final path proof. For selected candidates, perform bounded structural path/extent resolution on an authorized disposable logical snapshot using existing namespace/directory/inode/FileState/extent decoders and existing versioned record reader. Authenticate every demanded structural ObjectId; confirm the source checkpoint's retained root, regular inode, exact file length and ordered descriptor mapping to the selected target chunk. This is a selective proof of at most five final paths plus bounded rejected candidates, not another all-Store scanner. If reuse of the prior source mapping cannot be authenticated, stop and record the missing prerequisite; do not quietly implement another general storage engine or claim offsets from adjacency.

## Freeze one deterministic cohort and60 observations

The contract has five strata, candidate selected depth0..4. For depth0 require NATIVE_FULL, not a legacy FULL record; depths1..4 require NATIVE_PREFIX with that authenticated selected depth. A target must be a file-content chunk in the specific retained checkpoint, not merely metadata-value-only or final-union reachable. The corresponding source file must be regular and between4096B and8388608B inclusive.

Select lexicographically by source checkpoint index, raw relative path bytes, then offset. For each eligible occurrence require a4096B interval **wholly inside one authenticated extent span backed by the target ObjectId**, accounting for nonzero source_offset, truncated slices and extent logical_length. Raw chunk length>=4096 alone is insufficient. Earliest legal range start is the occurrence's logical file offset; do not select a straddling range and call the aggregate request one depth. Refuse traversal/symlink/absolute/NUL paths and authenticate mode/size/oracle. Seal path bytes using hex; do not assume UTF-8 Git paths. Multiple occurrences of the same chunk are valid candidate occurrences; choose deterministically and count a selected range once.

Prove the target selected locator was already admitted by the chosen checkpoint using authoritative pack-range/provenance plus retained root traversal; final pack IDs alone are not historical timestamps. In each arm bind its own Branch/Commit mapping for the same source SHA/tree. Control representation may be legacy FULL/DELTA; match file bytes/range/metadata, not candidate depth or locator. Require source-oracle full digest and selected4096B digest agreement. Neither oracle is a product-side data shortcut.

The full-containing-file read belongs to a **target-selected depth stratum**. Other file chunks may have other depths or be legacy. Record the file's selected representation composition from the prepared proof if available; never call the full file uniformly depth4 because one target chunk is depth4. Readahead can read neighboring chunks even for a wholly contained4096B request; native counters must retain all actual work.

Freeze60rows: depth0..4 × range/full × repetitions1,2,3 × twoarms. Loop strata then operation; within each repetition C/P, P/C, C/P. Exactly30observations per arm; digest verification is a separate action per observation, not another performance row. One disposable logical Store copy per arm, with fresh host process, Client/Store connection, container/daemon, fork and FUSE Workspace for every observation. Source/copy hash preparation is outside timing and warms OS caches; no OS-cold claim. No best-of or replacement after failure.

Before first timing, seal cohort-manifest.json with five selected checkpoint/path/offset/length tuples; exact sourceSHA/tree/oracle and both LayerFS mappings; selected candidate ObjectId/location/depth/closure; source-content and selective structural/dependency proof references; deterministic selection rule and rejected-candidate reasons; expected range/full digests; tool/dependency/producer/image identities; the complete60-row order and scope/status/units. If any stratum lacks an eligible proven range, stop before timing and report that finite cohort limitation, without substitution or extra target search beyond the fixed deterministic rule.

## Minimal sequential implementation tasks and ownership

A. Cohort-selection owner: new analysis-only `depth-read/select_cohort.py` and, only if necessary, a small Rust structural-proof entrypoint using existing decoders. Own candidate discovery/proof and cohort-manifest schema. Query the existing read-only analysis index and existing sealed blob/chunk mapping, then walk only needed retained structural paths. Do not edit the census or add path dumps to old inventories. Establish maximum candidate count/structural-read allowance prospectively; reaching it returns an explicit preparation limitation rather than a guessed cohort. Need not decode payloads repeatedly: exact target/base proof already exists upstream, while final source-range digests can use the sealed source blob after path proof.

B. Probe owner: new versioned cohort-aware analysis probe directory copied from the old read-probe implementation, leaving old source immutable. Replace hardcoded case/checkpoint/file/offset inputs with one verified manifest row and arm. Match both source trees exactly and validate numeric bounds/path before mount/read. Prefer safe argv transport: pass raw path as a separate OsString from hex-decoded bytes to Bash positional arguments (`dd if="$1" ...`) rather than interpolating Git paths into a shell program. All command structure/options remain frozen; no raw bytes in stdout/logs. Keep existing read/digest stdin handshake, public Exec through terminal output-drain timer, intrinsic decoder authentication, successful CleanEnd, unchanged head and zero active owners. No product/normal benchmark/fixture source change.

C. Orchestration/custody owner: new depth-read/run.py using existing runtime/container/copy helpers. Replace smoke validation with completed frozen full157 manifests and five-row cohort proof, two copies rather than four, exact60-row order, and4h campaign deadline. Reuse safe cleanup (unconditional owned container removal despite log/observer failure), cross-arm sealed utility equality, host lifetime/current RSS checks, strict source/image/product/binary/contract/cohort seals and original-source preservation. Do not place an output beneath either original or old immutable analysis directory. Scope all mutable fork/container-control/runtime metadata to the disposable copies.

D. Independent reviewer: owns only review/decision report and compact product-free fixture tests of selector ordering/path safety/span boundaries/pair mapping/cardinality/null handling. Challenge orphan or metadata-only targets, first-retained versus admitted provenance, source encoding assumptions, changed probe versus frozen producer identities, wrong timer/FUSE counter scope, and full-file mixed-depth interpretation. No duplicate Store/index scan. Root alone owns serialized builds, preflight and execution scheduling.

E. Build and proof gate, then execution: first resolve source/API adaptation and codec/link dependency identity before promising old executable reuse. Generate one locked probe dependency graph; compile identical new probe source once against each exact unchanged frozen product crate checkout. Hash both executables/tool sources/Cargo.lock/compiler provenance and verify source/product seals equal the corresponding producer/image. Reporter HEAD may differ; no relaxed identity checks. Reuse normal runtime images only when current strict image/source/product compatibility checks pass. Any altered product source blocks this diagnostic; do not rebuild a different arm under the same label.

Only after cohort correctness/review, both observer builds and full schedule are sealed does root run the60rows once under the normal measurement lock. After final row: verify original seals, closed copies/owner cleanup,60exact identities, every count/digest, counter/resource bounds and manifest conservation. Publish one read-cost retain/revise/reject result with raw ns/bytes/counts, medians/min-max, absolute worst-case deltas, small-sample and uncontrolled-cache limitations. Link previous construction/read regressions and the existing nine known Store-suite failures; no release/merge qualification. Complete issue88 disposition according to actual evidence; failure need not trigger an automatic new optimization experiment.

## Coverage and counter proof gates

Per-action physical/native counters are available as host Store receipt deltas. Require the prepared target's expected chain activity during the first measured read; a depth>0 candidate row with no PREFIX/dependency activity is invalid. But aggregate depth histograms include required targets and readahead/other chunks: do not equate one matching depth bin to proof of the exact target. Static retained path/span/locator proof supplies identity; dynamic counters supply evidence that the path wasn't bypassed or fully warmed. Define a lower bound consistent with one demanded target of depth d (at least one completed depth-d chain and at least d native edge visits/decode chain work), allowing additional work. If the implementation can satisfy a read through public read-ahead that transforms this lower bound, resolve the actual counter semantics in source/preflight before freezing; do not change the criterion after observations. Do not require equality to d across a full mixed-depth file.

FUSE public WorkspaceRead receipts arrive at End only. Per-action FUSE read/write fields stay null with reasons. Collect actual present read counts/bytes at CleanEnd under whole-session scope: setup mount proof+measured read+digest+end. Kernel-write counts are not available without Commit; this plan adds no Commit. Fixed read-only command shape, CleanEnd, unchanged namespace/head and no publication are correctness evidence, not a measured zero kernel-write counter. Existing mountinfo Bash proof verifies exact mount path+fuse type without warming file content.

Retain native reader hard bounds: at most4edges/five records, rawclosure<=1MiB, requested/read/decoded and actual ownership limits, no dynamic cache or allowance. Report all observed host CPU/I/O/RSS and cgroup categories with their exact lifetime/boundary scopes. Container total memory is not RSS; absent blockIO/free-disk fields are null unless prospectively added to this observer only before matching builds/samples.

## Stop conditions and finality

Stop before timing on unknown source/build/cohort/alias/selection/manifest identity, missing eligible proven stratum, inability to reuse the exact existing decoder, or resource-bound preparation. During the singlecampaign stop on count/digest/chain-activity/chronology/ownership/mount/cleanup failure,30s read/digest,120s lifecycle,4h campaign,8GiB sampled/current and lifetime host peak,2CPU/2GiB/no-swap/256PID container,16GiB scoped runtime,32GiB owned outputs,50GiB free reserve. Preserve failed row plus cleanup attempts; never replace it with a favorable row. Reaching an external lock owner waits/records the blocked launch rather than overlapping work.

The roughly30% guidance is contextual, not an invented automatic threshold; absolute short-call and tail costs require explicit owner-readable judgment. The diagnostic's endpoint is a read-cost decision for the existing implementation. It does not authorize root-anchor substitution, deeper chains, new hints/matcher/codec policy, replay/re-encoding, namespace/ID changes, migration, rollout, PR merge or another indefinite diagnostic loop.
