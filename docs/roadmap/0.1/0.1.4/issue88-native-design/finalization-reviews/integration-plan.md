# Finite integration and disposition plan for issue88

Planning only, based on native-worktree HEAD0cdd2f3533dcfaef1ea8caf9f80720ff417240af. Sources reviewed: published findings/next-read contract, native regression and integration reviews, D regression addenda, current pack/read/admission source and standalone read-probe source. No Store/inventory access, build, test, measurement, product edit or publication was performed.

**The frozen C+S1 versus C+S1+P implementation experiment is complete.** Both arms passed all157 historical checks, cleanup, single censuses,158-row cohort checks and physical reconciliation. Earlier review text describing candidate verification as pending is superseded by the published completed findings. The remaining issue-level decision is explicit owner disposition and the one proposed depth-stratified public-read diagnostic. Neither more compression nor baseline fault-path repairs must be silently folded into the measured implementation.

## 1. Freeze the completed research checkpoint and its claims

Keep the measured producers2753453933c55ed7f93eb21619c235668a01ef4c andd4f26f0d16f0f91c1f75767cf699012b8794ac01 distinct from later reporting HEAD0cdd2f353. Preserve exact host/image/product/dirty-analysis-patch seals, original acknowledgement allocations, pre-verification snapshots, manifests, failed prelaunch lock receipts, corrections and negative observations. No repeated passing full157/census/codec run is needed merely to finish documentation.

Research claims already supported: coherent shared Store/CAS/publisher implementation;184598528-byte candidate allocation versus218116096-byte fresh control; unchanged canonical/file workload semantics under the verified corpus; bounded native format/dependencies as exercised. Qualifications remain: sequential pair rather than repeatable speedup; full historical verification Exec cost+23.19%; the24-row SDK read campaign exercised depth0 native FULL alongside legacy Directory-Init bulk; one large range outlier; no release/fault qualification; no backward readability by old binaries.

Deliverable: one concise issue disposition checklist linking the existing sealed report and evidence rather than rewriting historical artifacts. Closure of research does not close release/allocation issues or approve adoption.

## 2. Prepare only the already selected read diagnostic

Use `published/next-read-contract.md` as the finite scope: five actual selected depth strata0–4,4096-byte range and containing-file full read, three repetitions/arm,60 total observations, fixed C/P, P/C, C/P order. The deterministic selection rule and declared file-size/range limits are fixed before timing; do not choose by observed latency or replace an absent stratum.

The smallest necessary source boundary is standalone analysis/probe tooling:

- Add one bounded cohort-manifest builder using existing authenticated inventory/graph metadata and frozen source/oracle mappings. The candidate inventory does not itself contain filesystem paths or extent offsets. Reuse the already sealed prior extraction cache for candidate proposals, then perform bounded retained-path/extent proof against each frozen arm before sealing a selection; cached paths alone are not authoritative. Preserve the cache identity and stop on an unprovable/missing cohort rather than silently launching another census. It records source index/SHA/tree, matching per-arm Commit/layer mapping, path bytes, offset/length, expected digests and candidate locator/dependency proof. It must prove each selected4096-byte range actually intersects its declared dependency-bearing target, not merely a file that contains such a target elsewhere. Reuse canonical/reference metadata decoding already available; no second storage decoder or per-read Store scan.
- Adapt the standalone probe `issue88-native-analysis/read-probe/src/main.rs` around lines117–125,173 and181. It currently derives length from two hardcoded SDK cases, reports checkpoint3 and opens literal `file`. Replace only those cohort-input assumptions with a validated manifest row's mapping, byte-safe path, offset/count/file length and expected digest. Preserve the public Client/FUSE/Exec route, byte-count pipeline, separate digest verification, lifecycle, resource counters and ownership. Do not interpolate arbitrary path bytes unsafely into a shell command: pass a validated/quoted argument through the existing public execution mechanism, with no private read bypass.
- Adapt its `run.py` hardcoded `CASES`, `schedule`, checkpoint3 oracle lookup and copy-per-case logic. Load the sealed cohort instead; use one logical disposable copy per arm, validate every selected mapping/oracle, freeze all60 rows, and reuse existing resource/cleanup/seal helpers. Keep the old24-row campaign and its tool identity reproducible; use a separately sealed tool revision/artifact directory. No new benchmark family or product change is required.
- Build new probe binaries for the cohort-input adaptation; the existing hardcoded probe binaries cannot execute this contract unchanged. Freeze their own source/binary identities separately from the unchanged product sources. Reuse the same normal host/images and authenticated product/environment artifacts; do not reuse an old probe hash for a rebuilt tool, impersonate an old producer commit, override identity checks or rebuild a different arm silently. Any unavoidable identity/interface mismatch is resolved before observations, not patched during them.

Before execution, bounded tool checks must cover malformed cohort fields, byte-safe paths, range/file bounds, exact paired oracle/mapping equality, absent/wrong-depth strata, schedule cardinality/order and cleanup/error preservation. Reuse the already passing native codec/reader tests rather than rerunning them without changed code. The read diagnostic itself needs separate prospective execution authorization; this plan launches none.

Deliverables: reviewed cohort-input adapter, frozen cohort/schedule/source identities, and the existing prospective contract with only those concrete interface details finalized. No full157 replay, re-encoding, Store repack or encoding-policy mutation.

## 3. Execute once when authorized, then make one finite decision

Run under the shared measurement lock and frozen limits. Retain every observation and failure; no favorable replacements. Check actual native fetch/decode/depth/edge counters establish the intended selected population, complete byte-count/digest correctness, unchanged originals and owned-container cleanup. Keep public Exec, digest, setup/End/cleanup and observers separate, and disclose uncontrolled OS cache state. Counter success is not itself a public latency result.

Report raw observations, per-stratum medians and absolute worst-case differences with the small sample limitation. Use the owner's approximate30% foreground guidance as interpretation, not an invented pass threshold; judge short-call absolute time. End with retain/revise/reject for the frozen implementation's read-cost suitability. A valid negative result completes this diagnostic just as a favorable result does. It does not authorize an anchor-policy experiment.

Research issue closure is possible after this explicit decision and an owner-approved issue disposition. If the owner elects to stop now, the completed implementation milestone can instead be closed as scoped research with depth-qualified read adoption explicitly unclaimed and the named diagnostic transferred to a tracked follow-up. Do not silently declare that follow-up completed. Keeping issue88 open for a named pending diagnostic is a scope disposition, not a false blocked state.

## 4. Preserve and triage nine unresolved Store-suite failures

The suite remains **P62PASS/9FAIL/1ignored**, D49/9/1 and accepted43/9/1. Matching failure names justify a narrow research-continuation conclusion; they are not a green suite or an automatic waiver. The following are the exact nine names from the independent reports.

| Failure | Evidence and finite follow-up boundary |
|---|---|
|`layerstack::tests::late_direct_initialization_failure_publishes_nothing`|Unexpected successful initialization in all three logs; generated IDs differ. Establish that injection fires at the intended publication boundary before claiming a rollback test. The old object-count-derived statement ordinal is suspect under batched insertion.|
|`layerstack::tests::multi_batch_publication_failure_removes_admitted_objects`|Actual residue P19571, D19573, accepted19572 versus expected0. Earlier object-admission transactions survive failure of final publication; the final rollback cannot undo earlier commits. This is material unresolved cleanup/retention semantics, not harmless test noise. Decide the required behavior before changing either code or expected residue; preserve unequal results and do not attribute the difference to randomness without evidence.|
|`objects::tests::direct_admission_honors_every_frozen_count_boundary`|Same printed physical-encoding-reservation error. Resolve how count-boundary guarantees coexist with the2MiB physical owner. Do not raise a limit or dismiss the guard as stale merely to pass.|
|`objects::tests::partitioned_completed_files_share_direct_facts_and_keep_failures_private`|720896 versus1048576 concerns the spill-buffer partition after shared/per-partition reservations, not the canonical memory limit. Reconcile the assertion against the declared owner arithmetic; keep real resource and failure-privacy checks.|
|`objects::tests::shared_admission_keeps_every_object_transaction_below_the_frozen_bounds`|Same physical-encoding-reservation error; investigate with the other boundary fixture but retain both count and byte constraints. A real batching limitation needs its own product fix/identity.|
|`objects::tests::spilled_candidate_visits_selected_objects_in_graph_order`|Same two selected IDs in reversed order. Accepted fixture mixes supplied reachable order with a conflicting consumed-order expectation. Establish one documented ordering contract before repairing the assertion; do not weaken object membership/authentication.|
|`objects::tests::workspace_delivery_selects_before_admission_and_deduplicates_across_phases`|Original three logs had an opaque injected-error assertion. Later test-only detailed reruns show unexpected success in P and accepted, not a hidden resource error. D's actual result remains unavailable because D was not rerun. Prove injection reaches the actual worker/thread and transaction before using the test as fault coverage.|
|`statements::tests::exact_manifest_prepares_against_exact_v6_schema`|Two semicolons counted instead of one; accepted SQL's comment contains the extra semicolon. Repair the test's actual-statement validation, not the SQL semantics or manifest guard.|
|`workspace::tests::snapshot_cache_reads_only_requested_authenticated_objects_and_reuses_them`|`InvalidParameterCount(2,5)` from the fixture's old `(id,bytes)` insertion against the selected-locator schema. Use a valid existing admission/locator fixture while retaining requested-only authentication and cache reuse assertions.|

The independent P comparison reports seven substantive panic payload matches after removing source line/column, not full behavioral equivalence. The differing residue and namespace identities remain explicit. The ignored large-spill case remains unexecuted, not implicitly passed.

Exact provenance already published in `issue88-native-design/regression-review.md`: P `issue88-P-custody-1/store-regression-1.log` SHA38f714ac4dfa8650fb0e3038c1429f7e0de08f8f0c8bf102c6dca3308f2c9efe; D `issue88-diagnostic-custody-1/store-regression-1.log` SHA9617f8bd310e818e64137b8c1d902bc1af6f4bd2a82476e34c4a6aa96d57068d; accepted `accepted-store-regression-1.log` SHA9e40aae8f594b004a9eaa9a5ca4a8150b0c0af270c1cf82906a9dcb74343075b. Detailed P/accepted workspace-delivery failures are preserved in their separate `*-delivery-failure-detail-1.log` receipts. This planning review read reports/source, not rerun logs/tests.

Disposition: these are explicit adoption/release/fault-qualification blockers until individually resolved or narrowly dispositioned by the owner against the actual contract. They are tracked follow-ups for normal-path research closure because the frozen public correctness/resource/cleanup checks passed and the research claim excludes those fault paths. Any new failure or normal-path integrity regression blocks the corresponding new action; matching historical names do not grant immunity. Do not bundle repairs into the frozen read representation, silently waive failures or rerun until nine disappear.

## 5. Compatibility, deletion and project workflow

Current compatibility is precise: new code reads version1 and version2; old readers reject native packs; canonical IDs and SQL schema remain unchanged; fresh research Stores only. No migration, downgrade safety, old-writer compatibility, rollout or recovery qualification has been established. Before adoption, specify which binaries may open/write native Stores and how an attempted downgrade is rejected. Pack-level read rejection alone does not prove an old writer is prevented from opening a same-SQL-schema Store. That is a compatibility design/check obligation, not an authorization to add a migration or new store format now.

Keep the conservative Directory-Init provenance limitation explicit. Fixing its source marker would change which initial objects and bases get native encoding and is a different treatment. The actual full157 Empty-Init/import route is validated; universal interface coverage is not claimed.

No production deletion is needed to close research or adapt the read probe. The legacy parser, COPY/INSERT reconstruction and structural matcher still have live version1/S1 callers. Native provenance's FILE bit, first-owner handling and private spill transport are semantic admission inputs despite diagnostic naming; removing observation counters must not remove that source fact. Keep canonical CAS operands, strict version dispatch, native bounds and required-versus-optional error handling. Do not remove old sealed analysis sources or evidence. The only immediate simplification is replacing the new probe adapter's hardcoded case/checkpoint assumptions with its one sealed cohort input, without creating a generic registry or second read implementation.

Use the existing `codex/` branch/worktree workflow for the narrowly scoped read-tool follow-up, preserving current unrelated edits. Commit and push only authorized tooling/report changes with their exact source identity and an additive issue88 summary. A draft PR may make review concrete if the owner wants one, but the PR description must label the completed experiment, retained negative results, failed full suite, compatibility limits and unperformed adoption gates. Do not merge, deploy, migrate or close release/allocation issues. No further optimization, parameter sweep, full157 repetition, cloud/S3/M5 work or global-search/deeper-chain change belongs to this finite plan.
