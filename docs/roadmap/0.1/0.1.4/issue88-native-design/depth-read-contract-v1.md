# Authorized issue88 depth-stratified public-read diagnostic

Prospective amendment to the sealed next-read-contract and finalization-plan.
Current user instruction authorizes implementation, isolated analysis builds,
bounded preparation, this one campaign, reporting and terminal issue disposition.
This is diagnostic evidence, admission_eligible=false, not a new benchmark family
or release qualification. No product/workload/encoding changes are permitted.

## Frozen claims and observations

Compare the existing control C+S1 and candidate C+S1+native payload through public
SDK fork/mount/FUSE/Exec/read-count/output-drain/End. Preserve the184598528B Store
result and existing verification/read regressions. Question: are observed range
and full-file public-read costs worthwhile for five explicitly selected native
depth strata? No population tail, worst-case, cold-cache, causal depth slope or
universal adoption claim is permitted. Valid negative evidence ends the campaign;
no automatic optimization or replacement sample.

The selected cohort is native FULL depth0 and native PREFIX depths1–4, one file
occurrence per stratum. Deterministic order: source checkpoint index, raw path
bytes, logical file offset. Regular file size4096..8388608B inclusive; range4096B
wholly within the target's authenticated extent descriptor after source_offset
and logical_length mapping. Match both arms' source SHA/tree/oracle, file bytes,
metadata and exact ranges. Full reads are target-selected strata with potentially
mixed-depth contents. Selected locator chronology needs actual acknowledgement
pack provenance plus retained-root proof, not final pack ID order alone.

## Preparation limits frozen before selection

- At most64 shortlisted candidate-path proof attempts in total.
- Each proof process: at most4096 structural ObjectSource reads and16777216 returned
  canonical structural bytes; at most4096 extent descriptors per file;120s wall.
- At most67108864 source-blob bytes read for full/range digest verification in total;
  retain digest reuse provenance if the same authenticated blob is encountered again.
- Whole cohort selection:1800s elapsed,134217728B analysis output/spill ceiling.
- At least50GiB host free space,8GiB host observed current/lifetime RSS limit.
- No unbounded scan/retry after a limit. Cap exhaustion is a preparation limitation,
  not an empty population or product failure.

Use existing sealed per-blob extraction metadata and source manifests to shortlist;
authenticate cache, exact canonical framing/CDC and source identity. Final census
edges lack path labels/extent offsets and cannot alone prove the range. Reuse
public LayerStackStore ObjectSource and exact filesystem/extent APIs for bounded
selected-path proof; no independent general decoder or repeated whole-Store census.
Canonical target/dependency proof and source-range digest must agree.

Create exactly one independent logical disposable Store copy per arm, reused by
preparation and the campaign. Originals and sealed pre-verification snapshots are
never opened by mutable SQLite/SDK. Check no owners before copying/opening; proof
processes close before campaign ownership begins. Record source/copy logical hashes,
proof before/after seals and any metadata effects separately. Copy allocation is
ineligible as Store allocation. A mutated namespace or unexpected proof mutation
stops preparation; no silent reset or new copy masks it.

## Probe/source identities

Reuse the prior standalone public read-probe implementation in a versioned
analysis-only depth-read directory. Existing hardcoded fixture executables cannot
run this cohort unchanged. New identical probe sources/dependencies are built
separately against each unchanged product arm; new executable and tool hashes,
compiler/features/profile/lockfile/build checkout seals are explicit. Normal
producer binaries/images remain frozen references. P's analysis-only dirty image
build qualification is preserved. Product/source mismatch stops, never overrides.

Control producer2753453933c55ed7f93eb21619c235668a01ef4c;
candidate d4f26f0d16f0f91c1f75767cf699012b8794ac01.
Original full schedule and all relevant input/binary/image hashes are retained in
new preparation custody before any selected-path proof. The concrete cohort and
60-row schedule, actual new probe identities and reviewed dynamic coverage rules
must be sealed before any timed read. Root coordinates one shared measurement lock;
source authoring/review may proceed concurrently, builds and scans may not.

## One fixed campaign

5 strata ×2 operations(range/full) ×3 repetitions ×2 arms =60 timed read rows,
30 per arm. Iterate depth0..4 then range/full; arm order by repetition C/P,P/C,C/P.
One digest action per read is separate verification, not a performance row.
Fresh host process/container/daemon/FUSE/session per observation. No Commit,
per-row Store copy, construction replay, recompression or changed representation.
Byte-safe paths are decoded from hex into separate command arguments; never
interpolated shell source. Fixed read-only dd/count and separate digest commands
run through the public Exec interface; no private Store read shortcut in timing.

Measure monotonic public Exec-through-terminal-output-drain elapsed ns. Setup,
fork/mount, argument construction, digest, scalar result checking, End/cleanup,
resource observation, copying/hashing/proofs and enclosing invocation are separate.
Host Store physical counters are operation deltas; FUSE counters available at End
remain whole-session. Missing per-phase/write metrics are null with reason, not0.
Static retained-path/locator proof plus runtime native decode/dependency activity
must establish intended coverage. Aggregate bins alone are not object identity.
Readahead/other chunks can add work; do not demand a homogeneous-depth full file.

Preserve container2CPU/2GiB/no-swap/256PID; host8GiB observed current/lifetime RSS;
30s read/digest;120s lifecycle;4h campaign;16GiB scoped runtime;32GiB owned outputs;
50GiB free reserve; all native reader byte/depth/ownership bounds. Cgroup total is
not RSS; peaks/cumulative/boundary observations retain actual scope. Runtime may
already include Store/spool. Fresh applications and hashing do not imply OS-cold
cache; no flush or cold claim. All unavailable fields are explicitly named.

Stop on identity/cohort/oracle/dependency/resource/cleanup failure; preserve raw
failed rows, unrun slots, stderr and cleanup attempts. No outlier exclusion, added
warmup, alternate range or best-of rerun. External lock rejection before a sample
is recorded as such and respected, not bypassed.

## Decision, debt and closure

Report each cell's three raw values, median,min/max, paired absolute and percentage
differences and scopes. Largest observed value is not worst-case latency. Roughly
30% foreground guidance is context with absolute short-call judgment, not an
invented numeric pass gate. Retain all existing unfavorable observations.

Independent review must reconcile correctness/custody, timing/resource coverage,
all60 declared identities (or explicit terminal negative), and the nine baseline
failure ledger including exact differing cleanup residues and opaque-result limits.
Keep Directory Init provenance and old-reader/old-writer/downgrade limits explicit.
No baseline repairs, product deletion or compatibility rollout is bundled.

Valid complete or valid product-negative evidence with completed owned cleanup and
explicit retain/revise/reject research decision may close #88 under user authority.
Invalid identity/cohort/infrastructure evidence supplies no product conclusion and
keeps the exact blocker open. No further optimization, migration, rollout, merge,
M5/cloud work, or closure of related release/allocation issues follows automatically.
