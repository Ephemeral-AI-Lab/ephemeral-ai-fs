# Issue105 prospective build experiment

Start: 2026-09-10T15:42:59.960739+00:00
Clean source be35274f14ae1bb9e796aabe48fc7f58dab3b603, expected branch verified.
All evidence is outside sealed issue104 campaign. Source snapshots are disposable detached git worktrees. No edits to original Rust or sealed evidence. Retain all attempts, no best-run selection.

Order / repetitions: one baseline first host qualification in fresh target (warm registry); one warm unchanged; one emitted-code Rust edit qualified build; one restore qualification; one intrusive diagnostic rebuild in separate target with cached compatible dependencies and Cargo fingerprint/timing plus rustc time-passes instrumentation; then candidate first, warm, same emitted-code edit, restore, and Python/docs-only qualification once each. One baseline and candidate image first/warm plus Python-family-only edit each; one candidate workload-input invalidation diagnostic if implementation changes workload build layering. No repeated tuning. A failed infrastructure attempt is retained and any replacement declared before execution.

Host command: python3 -u benchmark/fs-bench-pro/shared/runner.py --build-host. Image command: same runner --build-image. Timed boundary: immediately before child launch through child exit, including source seals, build, schema/format probes, hashes, archive, publication; external tee and progress included. Preparation/copy of snapshots and dependency seed outside commands reported separately. Runtime.run diagnostic phase observations are separate; unavailable linking metric null.

Representative emitted-code edit: benchmark/fs-bench-pro/src/main.rs, change one existing diagnostic string (exact diff recorded before first measurement); does not alter workload, timers or verifiers. Verify differing binary hash and restore byte identity; benchmark operation replay not needed. Historical issue104 unused dead-code function is documented separately and is not a changed-code identity guarantee.

Cache: fresh baseline native targets, installed Cargo registry warm. Candidate dependency reuse may copy ONLY proven-compatible dependencies to independent source-isolated targets, never benchmark output/fingerprints; full source/key/config validation remains. Preserve all existing caches. Every sensitive command uses existing runner-owned measurement lock; diagnostic parent acquires it only when child does not.

Acceptance: linked schema10/content107 PASS, restored host and unchanged Linux executable hashes match their originals; actual Rust rebuild visible; unchanged/Python/docs reuse; input/config/flags/features/toolchain invalidation focused checks pass. Prefer <=10 s unchanged/small-edit; otherwise BUILD_SLOW with measured dominant remaining cost. No compiler/profile/optimization/LTO/codegen/debug/incremental/linker changes in qualified output. No product changes. One bounded implementation pass: integrate proven dependency reuse using existing helper pattern if warranted; separate Docker workload compilation by actual input layers. No new cache framework. Product campaign evidence unaffected only if executable and product compatibility proven.
