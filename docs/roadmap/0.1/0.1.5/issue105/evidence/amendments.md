Before diagnostic/candidate measurements: precise launch UTC now sampled beside monotonic start. Earlier baseline UTC starts include pre-timer git status collection; wall_ns always starts immediately before Popen. No earlier observations changed. Docker workload also includes family Rust via include!, so copy families into the workload layer and omit host-only family Python/shell files through .dockerignore. No runtime/compiler input omitted.
Before candidate build: keep image archive/hash work inside existing runner measurement lock; focused regression checks verify lock scope. Candidate measured with the frozen code in candidate.patch plus separately retained new test_build_reuse.py. No native Rust edits persisted.
Before Python/docs invalidation check: add one final unchanged host qualification after restoring those files, so published full-source identity matches the restored snapshot. This is custody restoration, not a replacement of the declared warm observation.
