# Qualified j8 implementation proof
User authorized aggressive removal of redundant build work after cold root-cause test. Default host workers min(8, logical CPUs), explicit CARGO_BUILD_JOBS range 1..8, actual jobs in both compatibility seals. Remove duplicate native/dependency input traversal and compiler-version subprocesses by computing both hashes in one pass; before/after hash equality required. No compiler/profile/feature/codegen/linker changes.
One first qualified build in declared empty native target, one warm unchanged, one exact emitted diagnostic Rust edit in disposable detached source, one restoration qualification. Same source path as prior issue105 experiment cannot be modified because sealed; new worktree and independent cache, no dependency seeds for first invocation. One additional Python/docs-only check and final restoration if needed. Existing runner acquires lock alone; no overlap. Every attempt and exit retained, no best-run selection. Run focused tests once after final helper edits.
