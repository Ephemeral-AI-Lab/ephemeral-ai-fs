# Cold host root-cause diagnosis
One empty-target Cargo diagnostic at current clean ada559b9b commit, with --timings and /usr/bin/time -l. Same Rust 1.85.1, --locked --release -j2, fs-benchmark-pro + layerfs-layerstack-store --bins. No flags/profile changes, wrapper, bootstrap or cache seed. Registry/OS caches warm. Parent holds runner measurement lock; child Cargo and linked-probe functions do not reacquire. Capture exact commands, stdout/stderr, exit, wall, CPU/memory, Cargo timing HTML, source/binary hashes and linked schema10/content107 probes. No product benchmarks. Diagnose critical path, duplicate units, build scripts, target selection and available parallelism before suggesting improvement. One additional fresh-target diagnostic at a different jobs limit is permitted only after the first trace supports the scheduling hypothesis; declare the limit prospectively and label output compatibility before recommending adoption. No production changes in this root-cause pass.

## j8 hypothesis frozen before execution
2026-09-10T16:07 UTC: j2 used 211.59 CPU s / 116.49 wall = 1.816 cores; ready jobs wait 79.36 s. Test exactly one empty-target j8 build. Sole intended difference is jobs=8; same source, toolchain, dependency inputs, features, optimization and target selection. Cargo timing diagnostics unchanged. Compare linked probes and both executable SHA256s. No production default change before root-cause report.
