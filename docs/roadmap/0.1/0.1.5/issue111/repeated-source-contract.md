# Repeated LayerFS source scaling: prospective contract

Owner explicitly selected repetition of LayerFS files as a synthetic deduplication/scaling test at1,000/10,000/100,000 files (last tier clarified as100,000). Reuse sealed actual source snapshot run-20260911T010317Z, manifest7ec68f2c10df421f4df6d1136bae643574ec34ee9bbc57b9cf67a543e346aca2,387 files. Cases are separate from random/text namespace fixtures and the original cold2.7s gate.

For index j, source=sorted manifest[j%387], destination=copy-{j//387:06d}/original-relative-path. Ordinary independent byte copies, no hardlinks, clonefile, sparse holes, generated padding or content edits. Copy original modes/mtimes. Report exact directories/logical bytes and unique content per plan.json. Counts/bytes: [(1000, 12584955, 237), (10000, 129176473, 2322), (100000, 1292971697, 23262)]. All tiers contain385 distinct file contents /5,005,179 unique bytes. This deliberately favorable duplication is structural/synthetic evidence, not a production compression forecast or unique-data throughput/size qualification; do not publish logical-MB/s headlines.

Freeze n=3 fresh independent Stores per tier, in ascending tier order, public Client::initialize_layerstack Directory using unchanged qualified repository-init harness/product. Physical byte preparation, fsync/read-only shared mmap/touch/MS_SYNC|MS_INVALIDATE/unmap/close, oracle parsing, counters/census and Store closure outside Init timer. Record cache acquisition and actual process read bytes per sample; classify from reads against observed source allocation, never infer cold from reused-first label. Keep any cache-resident/mixed observations separately rather than dropping or pooling them. No timing-based reruns; preserve all attempts.

After each tier's three samples, reopen each measured Store in a separate process and enumerate/hash every stored file against independently generated Python SHA256 oracle. Full-content verification of the largest repeated1.293GB corpus has a prospectively declared extended180s bound per proof; performance outer310s. Distinguish verification time/resources from Init. Resource guard: zero swap/OOM, no live runtime/Workspace creation, cleanup checks. This is host-native public Init plus authenticated-reader verification, not a FUSE/concurrency/crash test.

Runner measurement lock for build/preparation/sample/verification; no concurrent resource-sensitive work or child lock reacquisition. Seal product/source before and after; no product edits, compaction/VACUUM/repack/GC or work shifted outside Init. Retain raw receipts, all exits, fixture manifests, all measured Stores and final hash manifest. Report per-tier n, within-cache-profile median/range, allocated/apparent/canonical bytes, exact-duplicate logical byte proportions and limits. No attempt to isolate pure compression versus exact deduplication without a same-input feature ablation. Evidence: `/Users/yifanxu/Ephemeral-AI-Lab/layerfs-repeated-source-evidence/20260911T011119Z`.
