# Fingerprint index plain qualification: invalid-cell replacement v2

Frozen before corrected timing; campaign identity fingerprint-plain-original-input-v2.
Original owner protocol permits one bounded replacement of an affected pair/cell
for demonstrated infrastructure invalidity. This uses the whole-cell allowance
once, superseding the experiment's narrower pair-only contingency for a uniformly
invalid input cell. No earlier replacement pair was run. No product optimization
or timing-selected retry is added.

Evidence: /tmp/layerfs-fingerprint-index-root points to
/Users/yifanxu/Ephemeral-AI-Lab/layerfs-fingerprint-index-evidence/20260911T092737Z.
The six initial plain attempts all used cp -cR fixture copies with changed directory
mtimes. Sampled original directory mtime was1700000000000000000; cloned directory
mtimes were1789120080945726351 and1789120104819303069. File bytes/modes were unchanged.
Every resulting Store had116454 canonical objects/513393058B, not112451/513026835B.
The independent namespace100 proof rejected mode/mtime. These input errors invalidate
all six timings as the declared fixture, including the two narrow cold-guard-eligible
candidate rows. Four rows additionally failed zero-residency. Preserve raw statuses
and apply the stronger whole-fixture invalidity in the derived report; never relabel
those rows as qualified or pool them with replacement data.

Replace the ENTIRE case cell, all six runs: C1,T1,T2,C2,C3,T3, same seed1, exact
plain binaries and matching images, same public operation, timers, acquisition,
CPU/memory treatment, thresholds and no warm-up. Use original prepared fixture
from main benchmark-results (as in the successful diagnostic screen), through the
identical registered main runner with its supported --host-binary selection.
Assert main harness bytes equal both sealed plain harnesses. Before each run,
check file and directory modes/mtimes/counts against the declared fixture. The
runner still independently verifies/invalidates/mincore-checks data pages and
fails closed. No edits to original fixture data or previously sampled copied inputs.

No new product/harness source edits, larger cache, weaker proof or change of target.
Plain comparison requires all cold.compare checks PLUS exact canonical counts and
the metadata audit. Preserve cache profile and disk-read bytes per row. The
meaningful-gain gate remains median paired reduction>=150ms, all pairs positive,
and at least2/3 pairs>=150ms; cold absolute gate remains<=2700000000ns.
If this single replacement fails, stop performance qualification and retain the
unpromoted candidate. No further cells/pairs/arms may be replaced in this experiment.
