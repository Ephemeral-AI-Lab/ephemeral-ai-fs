# Real source namespace100 Init contract

Follow-up requested by the owner:100 real source files. Separate diagnostic case `layerfs-source-100-init-v1`, not the registered pseudorandom `namespace-100-compact-v3`5MB case. Deterministic subset of the sealed LayerFS source385-file snapshot: sorted indices floor(i*384/99), i=0..99.100 files,1,076,513 logical bytes,99 small files and1 large file; no repeated/padded/rewritten content or selection by compressibility.

Manifest SHA256:`7dde0e19ada9e23d06e696310e496ed1ba50e27591cf3ca50607c44f7987983c`. Parent manifest SHA256:`c6534deb6a187a3a7da09a062684f6658a78575958b55b6d676e86b60cc0f2c6`. Input provenance and exclusions inherit [real-source-contract.md](real-source-contract.md). Evidence root:`/Users/yifanxu/Ephemeral-AI-Lab/layerfs-namespace100-evidence/run-20260911T002827Z`.

Freeze n=3 sequential cold samples before timing, fresh independent empty Stores, exact public Client::initialize_layerstack(Directory(input)), unchanged promoted-uncompacted product. Same qualified repository-init harness as385-file run; no product/code changes. Cache profile reused-first-sample-uncontrolled, each input read-only shared-mapped/touched/MS_SYNC|MS_INVALIDATE/unmapped before every sample; all descriptors/mappings closed. Report initialization disk read bytes per sample and require >=logical input bytes for cold validation. Preparation, oracle reads, counters, Store closure and full verification excluded from Init timer.

After all performance samples, independently reopen each Store in a fresh process and enumerate/stream/hash every stored file against the independently generated frozen Python SHA256 oracle. Require100 files and1,076,513 bytes per Store. Preserve resource/cleanup receipts; no swaps. Report n/median/range and allocated/apparent/canonical bytes; no paired improvement claim or synthetic2.7s gate. Existing45s verifier and310s outer operation bounds. Runner measurement lock, no overlap/double acquisition. Retain all attempts; no selective outlier deletion. One whole affected cell replacement only for demonstrated infrastructure invalidity. No compaction/VACUUM/repack/GC or moving Init work outside its timer.
