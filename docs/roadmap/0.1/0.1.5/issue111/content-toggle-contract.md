# Namespace content toggle: prospective contract (#111)

Owner requests pseudorandom enabled/disabled; disabled explicitly means generated structured text with identical file counts and byte sizes.

Existing default pseudorandom profiles and all four default registry IDs remain byte-identical. Add optional --pseudorandom and --no-pseudorandom to shared performance/setup/verification parser for explicit init_namespace cases. Disabled maps the canonical base case to a distinct `<base>-text-v1` ID. Text variants require explicit selection and do not expand the mandatory four-case registry. Reject other families, missing explicit cases and contradictory explicit enabled/text IDs. Record effective content choice and distinct case/fixture identities; cache and proof compatibility must never cross modes.

Structured text v1: deterministic repeated human-readable records containing file identity, size and incrementing record number. It is synthetic compressible text, not a real production corpus and not zero-filled data. Preserve base path/class/size permutation, exact byte totals, file/directory metadata, edit offset/marker and public operation. Generator is streaming/bounded and independent of caller fill boundaries. Small/large product dispatch remains based on raw file size. No product/storage/compression-policy changes.

Before timing freeze execution: seed1, n1 performance+standard independent proof for existing100/on and all four new off variants. No product speedup claim between different corpora, no pooling, no use of off for old cold2.7s gate. Standard prepared-fixture acquisition; report cache profile and actual read bytes per sample, never infer cold from labels.300s product/310s outer/600s setup; verifier45s/59s. Every attempt retained; one whole affected cell replacement only for demonstrated infrastructure failure. No outlier deletion. Runner measurement lock for builds/performance/proofs; no concurrency or child double-lock. Qualified host/image builds after final code changes. Preserve original dirty compaction-removal work.

Checks: old100 fixture digest c3ff9877a7edce88076b87d45b7da9ab7730836f10b4aff937f7b0cc82986ec1 unchanged; all four off plans have exactly the same per-file paths/classes/sizes as their base; text deterministic, differs across files and from pseudorandom, invariant to chunk boundaries; CLI normalization/defaults/rejection; mode-specific cache/input/proof identities; live verification through matching Linux workload generator. Existing registry self-checks stay four cases.
