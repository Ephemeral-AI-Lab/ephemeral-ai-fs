# historical_access v2 — complete worst-example coverage

Prospective extension after v1 qualification, before v2 implementation or
collection. No limits or correctness checks are relaxed. v1's ten passes remain
retained diagnostics on their exact source; v2 supersedes its registered fixture.

Use the already sealed public schema9 full157 Store. No history reconstruction
is performed. All ten v1 operations retain their semantics, cache profiles and
bounds, with IDs ending `-v2`. Their retained Commit identities now refer to this
full157 Store. The range-history cold/warm cases use the full157 worst adapter's
actual owner: checkpoint65, `pnpm-lock.yaml` [537371,543792),6421bytes. The original
checkpoint58 slice remains in retained v1 evidence, not silently relabeled.

Add exactly one case `ha-metadata-worst-v2`: checkpoint57, application-cold stat
of `.agents/notes/implemented/architecture/2026-07-19-gui-layering-and-rpc-protocol.zh.md`.
This inode is serial0x2414 in the worst metadata leaf
`6074b7f6407a06e4e70d66a97e826208b2e2a63b88e2b867c3877815dc561d62`.
The source-to-compact-root mapping and canonical inode table establish its
membership. This is the leaf whose offline closure decoded5,785,404bytes.
The native target is `30e63f70dcde8ed95e4a3bcb03c0a298d9b26725ac95a6cb106d8eda75a33609`,
owned by `2ce47b7786beeeba6ac90248fe04e54ccc105c2d6f21eb5844773a33a8e9933a`.

Exactly11 performance cases and11 separate proofs, selected serially. Each has
one hard15second complete envelope and12second working deadline; verification
also has its prospectively chosen15second watchdog. No change from v1 timing,
resource, cleanup, custody or claim restrictions. Start with small-head and
proof, then the remaining ten pairs once each. Retain every attempted result.

Read cases additionally check the original full-file type/mode/size and normalized
mtime after the pure read timer, so a bounded full-file read also rejects a
changed length. Stat and directory coverage is unchanged. Symlink/hardlink
mutation is outside this read-only case set; no reference semantics are rewritten.

These actual source paths/ranges cover the worst examples on the currently
supported public format. They cannot qualify the unintegrated offline9304 format
or establish that the public Store has identical amplification. #100 still owns
integration; #102 can rerun this same family against its supported candidate.
