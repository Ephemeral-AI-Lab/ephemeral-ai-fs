# SDK Store observation supplement v1

Prospective reporting repair before recollection. The12 edit_length_preserving
performance cases and12 proofs passed, but the performance receipts lack final
allocated Store bytes. Those timings remain immutable historical evidence and
are not sufficient for #104's complete allocation reporting.

After the native product command and resource window end, before cleanup, stat
the closed SDK payload/store.sqlite and any required -wal/-shm/-journal files.
Record path/device/inode, apparent length, allocated blocks*512, mtime and the
separately timed observation. No Store opening, digest, oracle, rewrite or query
is added to product timing. Report payload separately unavailable if unexposed.
The physical identity is an inode/device observation, not a fabricated content hash.

Shared runner change applies only to SDK performance receipts. Product/native
harness, fixtures, timers, seeds, workload and all verifier checks remain unchanged.
Recollect only the12 affected performance cases with missing allocation; retain
their existing PASS proofs, linking exact proof receipt/source/input/row IDs and
this applicability declaration. Other completed families retain their original
complete Store observations and are unaffected. Future SDK cases use the added
observation on their first attempt. No blanket restart or better-number rerun.

Verify the stat helper with a disposable database and sidecar; require the new
performance receipt before declaring SDK allocation coverage complete. Existing
SDK proof verification-only Store growth is unavailable, never zero. The original
proof's correctness/resource/cleanup scope is unchanged.
