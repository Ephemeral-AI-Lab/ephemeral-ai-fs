# Retained DELTA candidate diagnostic and candidate

Prospective amendment before changed encoding/measurement, 2026-09-10.
Retained-candidate-1 verifies ten states at 49,319,936 allocated bytes. It remains
4,319,936 bytes above target; no full157 confirmation is authorized at this size.

A metadata-only conservative subset identifies 1,213 remaining FULL targets /
2,478,312 current FULL frame bytes with same-basename previous-state DELTA bases
inside existing edge/closure bounds. These bases first appeared in the immediately
preceding state, so the new retained cache lifetime can expose them. This is
coverage, not a savings forecast; the earlier session-only DELTA experiment did
not have this cross-admission availability and remains rejected on its own evidence.

Five original fixture pairs were chosen by distinct basename, largest remaining
FULL in each family, and one highest namespace-parent-score old base. Hashes were
checked against prepared-input seals. Four pairs share fewer than the required
two fingerprints and do not justify trials. The tool-schema pair shares seven:
`snapshots/sdk/bash-tool/tool-schemas.expected.json` versus the preceding state's
`examples/acp-agent/tests/snapshots/product-subagent-both/tool-schemas.expected.json`.
The target currently has a 9,578-byte FULL frame; the actual old base is selected
as DELTA and cannot enter the retained-FULL cache. Keep the exact evidence in
`retained-delta-opportunity.json` and `retained-delta-pairs.json`. No codec result,
cache residency or product saving is inferred from these observations.

Apply the already focused-tested selected-chain policy to the new compact,
retained cache owner. Register actual selected SmallContent FULL and DELTA winners
only; no private unselected alternatives or payload cache. Same 1,024 records,
8,192 u16 references, 128-KiB active/idle ownership transfer and eight probes.
Genuine/removed-name hints retain priority. Authenticate the single cached candidate
with small_predecessor, using kind 1 for FULL and existing schema-9 kind 2 for a
bounded DELTA base. No additional candidate trial at a bound refusal. FULL fallback
and complete encoded-cost selection remain unchanged.

All original limits remain: 8 edges, 512 KiB decoded canonical closure including
target, 256 KiB retained encoded capacity, 2-MiB active reconstruction and 3-MiB
encoding with actual static codec/operand ownership. Decoder/encoder do not overlap.
Existing iterative authentication, location chronology and transitive physical
retention apply. Retained admission output already survives rollback; a failed or
abandoned admission discards inherited/private cache entries and removes only its
own newer packs. Transfer under the writer permit without cloning; reopen starts
empty. No new persisted interpretation, schema, global index, history scan, codec,
cutoff/page change or old-schema promotion is introduced.

Extend the focused handoff test to publish a DELTA in the first retained admission
and use it in the next, checking exact physical base, kind 2, complete bytes,
rollback and cold reopen. Then build and run one fresh ten-state performance,
frozen census, exact same-Store verifier and cleanup; compare complete allocations
and resource/speed costs against retained-FULL, removed-name and original controls.
This is a changed lifetime/base combination, not an unchanged rerun. Preserve its
patch and reject it if storage gains do not justify costs. Do not sweep cache sizes,
depths, score thresholds or codecs. Keep prospective speed/RSS working criteria,
honest target/near-target distinctions and the full157 gate.
