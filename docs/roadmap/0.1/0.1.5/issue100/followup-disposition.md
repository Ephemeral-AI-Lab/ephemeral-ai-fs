# Disposition after bounded-chain smoke

Chain-1 saves 9,437,184 allocated bytes, retaining a 56,668,160-byte Store with
all ten original states verified. It misses 45,000,000 by 11,668,160 bytes and is
not near-target. Keep this material storage improvement with its measured speed
tradeoffs; do not label it completion of the 45-MB objective.

The second, prospective 128-FULL session-local candidate diagnostic found **no
eligible sketch candidate in any of five relevant FULL target occurrences**
from the three real families. Examined window counts were 128, 2, 128, 128, 1.
The zero alternative totals in its log mean no candidate trial, not free target
storage. No product ring was implemented, no window/signature sweep was run,
and no frame saving is claimed. Same-snapshot existence alone did not establish
bounded admission availability. The broader read-only examples of renamed and
bilingual documents establish similarity but not this ring's effectiveness.

The selected fixture's manifests contain six CDC-to-small transitions, totalling
465,897 raw target bytes. The concrete translation example is one of them.
This is logical opportunity accounting, not physical savings or a bound on all
future descendants. It does not justify implementing a new extent/native-chain
closure reader to close the remaining 11.67-MB gap. Cross-CDC reader work is
therefore deferred; the original cutoff and full correct CDC path remain.

The recent diagnostic initially failed to compile because SQLite integer fields
were decoded as usize. It was corrected to checked i64 conversions and then
passed. Both sources/logs/commands and its patch are retained externally. The
isolated test/module wiring was removed afterwards; current product and harness
seals match the exact measured f63166766 binary/image. There is no relabelled
binary or new unmeasured product treatment.

These results do not prove 45 MB impossible for every allowed bounded design.
They do show that the measured chain plus the concrete bounded candidate policy
do not establish the target. No speculative percentages, larger limits, offline
future-base encoding, reclaimed-byte credit or metadata rewrite is substituted.

## Execution mistake: premature full157

The owner explicitly gated final full157 on a stable ten-snapshot candidate near
45 MB. The 56,668,160-byte result did not meet that gate. Starting full157 anyway
was an execution error: the agent incorrectly treated final confirmation as
unconditional and treated a useful partial improvement as short-loop completion.
The narrow unsuccessful candidate-ring sample also does not establish that other
bounded online candidate policies cannot close the gap.

On the owner's correction, the verifier was interrupted. Performance and census
had completed, but historical verification completed only 150/157 checkpoints.
The runner reports INCOMPLETE, forced coordinator stop, DIAGNOSTIC cleanup and
successful container removal. These artifacts are retained as premature,
incomplete diagnostics; they are not final confirmation or accepted full157 proof.
No further full157 work is permitted in this loop until the ten-snapshot result
is genuinely near 45 MB and the design is stable. Return to diagnosis of the
remaining FULL population and bounded available-base opportunity; do not close
the 45-MB objective or declare the revised scope exhausted from these samples.

Evidence root: `/Users/yifanxu/Ephemeral-AI-Lab/layerfs-issue100-45mb-evidence`:
`recent-diagnostic-1*`, `recent-diagnostic-2*`, `recent-diagnostic.patch`,
`cdc-to-small-opportunity.json`, `full157-source-reuse.json`.
