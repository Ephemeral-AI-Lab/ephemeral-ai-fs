# Retained implementation full157 confirmation

2026-09-10: the owner explicitly requested “run full 157 to see how it goes”.
This supersedes the earlier near-target gate for this run. It does not change
the ten-snapshot45,000,000-byte target, which remains unmet at49,319,936 bytes,
or authorize a release-admission PASS, tag or release.

Run all157 selected original full-history checkpoints using the retained product
measured at ee78028ba, byte-identical in product/harness to1955205db. Reuse its
saved host binary and immutable image ID after normal runner seal checks. Do not
relabel identities or rebuild unchanged artifacts. Use a fresh output/Store,
existing prepared source inputs and existing runner serialization/watchdogs.

The full157-execution-contract.md fixture, operation order, public boundaries,
resource caps and14400/300/120-second phase/operation/setup-cleanup bounds remain.
Its initial one-level policy is historical: current schema9 bounded chains,
removed-name discovery, compact retained-FULL cache and their specified ownership
are the treatment. Reuse the matched released full157 control under the qualified
harness applicability audit; no material full157 route/timer change has occurred.

Freeze performance allocation and Store digest, collect the read-only census,
then reopen that SAME Store in a fresh coordinator and verify all157 original
oracles. Require clean shutdown/container removal. Keep performance and historical
verification costs separate. Compare actual storage, public medians/sums/ranges,
CPU/RSS and walls against matched released control and initial v015. Retain the
existing prospective working speed criteria as comparisons, not numerical release
gates. The45-MB objective does not apply to157 retained states.
