# Frozen structural metadata protocol — 2026-09-10

Two declared diagnostics, no parameter sweep.

1. Exact D canonical inventory and previous-root candidate/group policy from the retained depth1 experiment. Permit selected DELTA bases up to16 edges and128KiB summed canonical closure. Keep8192-byte per-object cap, matcher budget, actual group rejection threshold, pinned codec, all metadata objects and all bases. Matched chronological FULL remains control. Physical FULL/DELTA record inventory and locators are persisted. Reconstruct every canonical object and compare all158 roots and directories exactly. Source seals must hold. Total store allocation belongs to parent integration, not metadata pack totals.

2. Structurally different reference: replace ONLY LFS6INT inode table objects with an initial full table plus sequential changed/deleted serial-value records and full checkpoints every16 roots. Keep every other canonical metadata object in the reference, even when potentially redundant. Store checkpoint/change records in independently compressed ≤16KiB blocks; maintain actual SQLite state/segment tables and indexes with per-state authentication digests and previous/checkpoint references. All retained non-table metadata pack bytes and locators counted. Reconstruct all158 ordered tables and directory semantics exactly against sealed D inventory. This reference provides semantic metadata storage, NOT identical D table object IDs or production random lookup; reconstruction may materialize a whole inode table and replay15 states. Report decoded byte work, index allocation, checkpoints, framing and retained costs. No public runtime claim. No state-dependent tuning.
