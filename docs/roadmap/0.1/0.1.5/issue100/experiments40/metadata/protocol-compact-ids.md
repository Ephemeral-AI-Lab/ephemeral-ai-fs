# Conditional experiment D — scoped8-byte stable inode serials

ABC artifacts are sealed in manifest.json; do not modify/re-encode their treatments. The fixed control is C's result.json metadata5,593,831B+object-index1,814,528B=7,408,359B. Run exactly one further treatment D and report separately, no parameter sweep.

D keeps C's inline73-byte inode values and direct-directory mappings but assigns each distinct original32-byte inode ID a unique u64 serial, starting1. Traverse eleven original roots in original admission order and their original sorted inode entries; assign at first appearance, reuse thereafter. Serialize serial big-endian8bytes. One32-byte scope identifier is declared in each namespace root, deterministically BLAKE3 of a diagnostic descriptor and original genesis root. Scope+serial is the new canonical stable identity; content and object hashes remain full32-byte authenticated IDs. The old-ID mapping exists solely as the diagnostic equivalence oracle, not a product dictionary required to read fresh-formatStores. Migrating old Stores would need translation/transientspooling and is unimplemented.

Rebuild every inode table sorted by serial, balanced leaf capacity100 (81bytes/entry) and branch capacity127 (40bytes/entry), original31-byte node summary framing, subtree bytes=count*81, new LFS6INT magic. Rewrite every directory mapping leaf's inode reference from32to8bytes, update subtree-byte summaries (10+name length perleafentry), recursively rehash all branch child IDs; preserve original directory page membership/levels and full32-byte branch objectrefs. New LFS6NSP magic. Inline directory inode content refs point to those new mapping roots. New namespace LFS6FSR: standard12-byte roleheader +profile32+scope32+rootserial8+tableroot32. No identity hash truncation or omitted referenced objects.

Persist allocation state explicitly: new standalone scope_allocator table with scope32 primarykey and INTEGER highwater, current final allocated serial. Charge its actual SQLite4096-byte dbstat page allocation plus schema/databasebytes in the D projection. This is global monotonic allocation across branches, not a counter reset from a historical namespace root. Actual transaction/publication allocator, imported foreign scopes and migration are not implemented. The focused diagnostic allocator check must reserve distinct values for new inodes in two branches with a shared ancestor; sharedinode/rename/hardlink identity stays unchanged. Range must stay within positive signed64-bitSQLite integer; no recycled serials.

Same exact legacy pinnedZstandard,16KiBFULL-onlyrole-magic/fullIDsorted grouping, packbounds and actual fullhashindex policy as ABC. Re-authenticate original bytes, validate codec equivalence, then encode onlyD. Count every new directory/inline/table/root object, metadata pack header, indexrow and allocatorpage. Canonical pages<=8192bytes; replacement leaves100*81+44=8144bytes. Verify all11states by mapping serials back to original inodeIDs and comparing exact kind/refcount/content/metadata,directorynames,mappingcounts/levels. Include focused stable-rename/sharedlinks/distinctinode/sharedancestor/newbranchallocation checks. PreserveABC manifest hashes and originalStore SHA before/after. D is a freshformat representation diagnostic, not productStore allocation or latency evidence.
