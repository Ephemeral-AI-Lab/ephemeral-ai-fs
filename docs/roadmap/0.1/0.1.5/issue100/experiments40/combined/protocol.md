# Combined fixed offline layout protocol

Written before combined construction. Two fixed treatments: D metadata plus SmallContent grammar B; D metadata plus grammar B plus the one selected chronological CDC family graph. This is not a sweep. Each starts from a fresh complete copy of the exact retained post-verification source, SHA256713e43e4f31a489c8eb347b953702ca97c33b17832fbdc0633018584308507f4, opened immutable read-only. Reuse the existing framing `offline-layout-3.sqlite` equally-VACUUMed baseline after checking its saved hash; do not rerun the baseline.

D: reconstruct the fixed metadata inventory through metadata/d_api.py. Charge every actual canonical object, pack, full32-byte hash locator and the32-byte scope/highwater allocator table. Replace all original v1 metadata objects/packs with the exact D inventory; D retains canonical references to unchanged content. Require all11 namespace root semantic checks and full inventory membership. Build metadata packs after the source maximum packID, preserving content chronology. No shortened content hashes.

Root identity cascade: metadata roots change. Verify every original LayerId and CommitId against the code's BLAKE3 derivation first. This fixture contains one genesis layer and ten commits. Derive the new genesis LayerId from unchanged stack ID and new root, then all ten new CommitIds in parent DAG order from new root/new parent/new base layer. Rewrite layer_stacks head layer, branches base layer/head commit, commits parent/base/root, layers parent/source/root and workspace references as applicable. Random branch/stack IDs and names remain unchanged. Abort on unexpected schema/object foreign keys or unresolved references. Record full old→new object-root, layer and commit mappings. External historical IDs are intentionally different under the canonical format change; semantic snapshots are compared instead.

Framing B: reencode all v3 SmallContent packs as isolated diagnostic magic/version102, retaining full content/base hashes, group mapping and exact frames. Reconstruct each v3 input byte-for-byte using the unchanged locator lengths. Authenticate all selected SmallContent objects/dependencies from records extracted from the assembled copy, retaining existing depth/closure/static decoder bounds.

CDC extension: use only the fixed family-result selected graph (249lockfile chunks,551895record bytes net saving), with exact recorded frames including12 newly FULL records. Preserve every other native record. Initially retain original pack/group/ordinal layout and rebuild per-group end directories and pack directories; enforce65536byte native groups,262144byte packs and original locator/chronology/resource bounds. If these limits cannot hold, stop this extension and report rather than silently adopt a different layout. Authenticate all735 native canonical IDs and closure bounds from the resulting physical graph, including unaffected dependents.

Each final SQLite copy uses an unsupported diagnostic user_version, retains schema constraints and full-width CAS locators, passes integrity_check and foreign_key_check, verifies every selected locator maps to exactly one physical record (no unlocated/orphan records), and performs applicable supplied metadata/framing/native semantic verification. Close, VACUUM, measure actual logical bytes and copy filesystem allocation; reconcile metadata/SmallContent/native pack bytes, SQLite nonpack bytes and allocation difference. Source SHA must remain unchanged. Verification uses standalone diagnostic decoders, not product open, historical checkout or rollback APIs. No live Store size, latency, release, public-format compatibility or migration claim.
