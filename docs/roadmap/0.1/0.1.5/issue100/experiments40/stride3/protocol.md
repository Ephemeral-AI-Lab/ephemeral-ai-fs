# Stride3 public LayerFS / matched Git comparison

Frozen2026-09-10 before either measurement.53original indices1,4,...157; scenario deepseek-stride3-v1, committed contract unchanged.

Measure the currently runnable retained public product (product seal24cde1dce88104daebf2d01e6b711665cfa52c52880b31157fbcfa5c27214673) through the new stride3 harness, fresh host-owned SQLite and Linux FUSE/workload only. This establishes the53-state public-product foundation. The79.79MB structural157 result is an offline format and is not silently substituted for a runnable candidate. Do not claim a stride3 structural-format result without a separate actual construction.

Use one fresh LayerFS performance run, freeze its allocation and receipts, verify all53selected states against the original oracles on that same Store, then retain cleanup/custody. No repeated passing runs. Build/setup, transfer, Commit, verification and cleanup have separate scopes. Matching source/product/image seals must pass. Existing10/157public results remain their original workloads; elapsed ratios across counts are descriptive, not an identical-workload speedup.

Construct separate native Git53 snapshot history from exactly the selected trees, with no unselected original commit history and no object alternates. Preserve original source Git and fixtures. Follow established compression6/window10/depth50/threads2 policy, retaining loose and packed measurements. Verify exact objects,53trees/parent links and every original selected path/content oracle. Git construction/repacking time is not comparable to the LayerFS public save/Commit timer.

Run resource-sensitive builds/performance/Git collection sequentially under the existing measurement lock. Record all errors, source/build identities and exact commands in fresh paths. Report total allocated storage and logical bytes,53Created outcomes, all53original-state verification, timing/resource scopes and complete teardown. Admission remains false. No numerical storage or speed target is introduced after measurement.
