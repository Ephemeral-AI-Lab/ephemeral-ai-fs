# Matched chronological metadata FULL vs depth1 complete-copy protocol

Written before new copy construction. Source is the prior complete full157 D+B+CDC diagnostic copy, SHA25620bc3581f29ecab3168712b8972f2eb24898f92d98e94d402657e953048efa6b. Its prior standalone independent oracle proof covers157 states,904143 path states and4936693030 logical file bytes. Source and all previous artifacts remain read-only.

Construct exactly two fresh complete copies: chronological FULL metadata control, and selected depth1 metadata delta treatment. Consume the sealed metadata/delta_api cached packs/locators without re-running encoder/candidate selection. Both have the same24748 canonical metadata objects,388 pack boundaries and5154 group memberships. Expected metadata pack bytes are35010506(FULL) and25624588(delta), difference9385918. Rebase all metadata pack numbers above current source maximum; this preserves strict earlier-pack bases. Replace only old v1metadata pack rows and metadata locator positions. Every canonical object ID/length, root, typed history ID, branch/stack/head/parent row and scope/highwater allocator remains identical. Native and SmallContent pack bytes and all78619 content locator rows must be exactly identical.

Decode/authenticate every new physical metadata record and compare canonical bytes with the prior actual D source and sealed inventory. Every delta must name a selected FULL earlier-pack inode-leaf base, have depth exactly1, canonical target/base each<=8192 and combined<=16384; authenticate both target and base. Require exact physical locator membership, no orphan records, unchanged SQL rows excluding physical packs/locators, foreign-key and SQLite integrity checks. Both copies keep the existing unsupported diagnostic schema9301: canonical formats and existing v1 physical delta grammar do not change. Current product compatibility remains unclaimed.

VACUUM each new copy once, close, hash, stat logical/allocated bytes and reconcile all pack categories, fullhash object index, remaining SQL and allocation difference. Compare delta to chronological FULL to isolate the fixed policy; also report difference from prior hash-sorted D copy separately. Reuse existing matched Git figures and prior source oracle proof by byte/canonical equality; do not rerun controls/content codecs or Git repack. Parent will independently run its existing157 oracle verifier with the new read-only delta-capable reader.

Expose a minimal StoreReader subclass over prior full157 actual-copy reader, extending only v1metadata kind1 decoding and strict FULL-base/chronology/role/size authentication checks. Keep prior bounded caches and content decoding unchanged. Validate representative correct metadata delta and missing/corrupt/non-FULL/wrong-role/future-base rejection before delivery. No product edits, sourceStore writes, prior artifact replacement, performance claims or further policy variants.
