# Fixed-policy chronological249-chunk family extension

Freeze before running. Extend only previous-file minhash policy from protocol2 over all249 retained pnpm-lock.yaml CDC chunk identities. Preserve every canonical chunk and all other families. Order by original first physical pack/group/record admission; original chunk spans, file versions and physical-step correspondence must match sealed originals and product scanner. No parameter, codec, cache-size or ranking sweep.

For each prior large lockfile version, build once the same bounded128entry/1MiBraw signature index, decoding its current simulated graph. This index cost is charged separately and is not free. For every target with a preceding large file, compare complete FULL(5+frame), original first overlap hint if eligible under the simulated graph, and at most ONE previous-file minhash winner (overlap>=2, lexicographicIDtie). Choose lowest complete record cost, prefer FULL on ties; first-overlap wins equal prefix costs. Do not choose a second-ranked similarity candidate if its winner is depth-ineligible. Step2 has no preceding large lockfile: preserve original representation and record the reason.

After each choice update that chunk's simulated physical encoding, then recompute dependency depth, raw closure and selected-record read work before any later candidate use. Preserve all original physical identities and charge each final record once. Changed FULL fallback and worsening targets remain in totals. No sum of isolated original-graph savings.

Decode+authenticate every chosen chunk and reconstruct all nine original large lockfile versions using the simulated graph, comparing their exact sealed bytes. Check canonical identities, chronology, cycles, maximum4edges,1MiBrawclosure and conservative per-target8lookup/512KiBencoded/decoded limits cumulatively across first-overlap then selected similarity. A read-budget rejection causes no extra ranked candidate. Original group counts are held for read-directory accounting; no new pack/group/SQL layout or allocation is generated. Missing original per-batch remaining512trial/8MiBallowances remain a disclosed product-integration uncertainty.

Report original vs final249record sum, FULL/PREFIX counts, improved/worsened/unchanged targets and newlyFULL bytes/depth effects. Preserve selected frames in the owned experimental directory so the graph can be rechecked. Index creation, candidate read/encode work and diagnostic walltimes are separate. No whole-Store or publicCommit result, no Store rewrite, smoke benchmark or product integration.
