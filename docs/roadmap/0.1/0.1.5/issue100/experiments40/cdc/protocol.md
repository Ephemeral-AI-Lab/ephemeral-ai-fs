# CDC experiment 2: prospective fixed protocol

Freeze before alternative encoding. Input is the post-verification retained-candidate-1 Store with SHA256 713e43e4f31a489c8eb347b953702ca97c33b17832fbdc0633018584308507f4 and the same ten sealed original fixture states. Read-only Store access, no product changes, no Store replay, no broad sweeps.

Select exactly twelve current pnpm-lock.yaml physical CDC records, descending current complete record byte length then object ID; eligibility for sample is first physical admission in selected steps 3–10 with a preceding large lockfile version. Do not prefilter by whether hints, helpful alternatives, or depth headroom exist. Once derived, save sample IDs and provenance before encoding alternatives.

Reconstruct each target's original CDC span from the frozen source scanner on the original sealed file. Authenticate every reconstructed canonical chunk against its selected object ID. Check first original appearance against actual first pack admission using the retained performance-step pack ranges. Reconstruct prior file chunk spans the same way. Derive up to four distinct overlap hints using the source cursor's exact interval rule and traversal order. Assert each selected PREFIX base equals the first hint; record no-hint/FULL cases separately. If this cannot reproduce provenance, report the mismatch and stop rather than inventing inputs.

Decode every actual/alternative base from its existing selected physical graph, checking canonical identity, chronology, no cycles, role, depth and closure bounds. Alternative candidates are the remaining at-most-three overlap hints only. Keep existing representations and base identities fixed. No new bases, cache, candidate search or rewrites.

Encoder must be Zstd1.5.7, use static aligned1MiB CCtx and exact native source setters: reset session+parameters; level3; windowLog20; content-size1; checksum1; dictionary-ID0; workers0; raw refPrefix; compress2. Require byte-for-byte reproduction of actual selected frames on all twelve targets before interpreting alternatives. Reproduce FULL alternative too. Use existing native32KiB raw,33024B frame, depth4 and1MiB raw closure limits, and report encoded/decoded dependency-group work against reader limits. Do not silently increase budgets.

Charge FULL as5+frame bytes and PREFIX as37+frame bytes, so references are included. Existing bases are retained once in the graph and are not counted as newly duplicated or removable. Pack/group/index allocation changes remain outside this target-record experiment. Existing group packing is frozen by FULL alternatives, so record savings alone are not a measured whole-Store saving.

Record per-candidate encode and decode/closure timings, logical bytes and read groups; these are diagnostic microtimings, not Commit latency. Decode every newly encoded frame against its authenticated base and compare exact target bytes. Report current-first-hint versus best eligible overlap-hint result, unchanged or rejected cases, and any eligibility limitations. If sample repeatedly helps, report to parent before extending a fixed policy over249 physical lockfile records. If overlap hints fail, stop and propose similarity fallback without running it.
