# Priority2 content representation: fixed53-state physical-copy experiment

Frozen before encoding on2026-09-10. Source is the verified Priority1 metadata candidate `/Users/yifanxu/Ephemeral-AI-Lab/layerfs-issue100-ordered-optimization/metadata/v2-53/candidate.sqlite`, SHA25654f47590d3f1189395b109901ab6b403c45074a9634cf7be3e28384365dfdb59, allocated57,974,784B. Preserve original metadata canonical identities, original SmallContent/nativechunk identities, every typed SQL row, schema9304, allocator and pool catalogue. No general index-policy change. Add necessary whole-file object rows to the existing object index; count their actual allocation.

Use the same fixed Git53 pack's actual blob DAG and pinned zstd1.5.7 level3/window18small/window20large/checksum/content-size. Population is every nonempty regular-file blob in53sealed original states:59,768SmallContent identities plus224largeversions; largestlarge raw1,241,221B. Omitted empty/symlink representations remain unchanged in metadata. A Git base outside this population becomes FULL. All FULL/newbase/displacedancestor outcomes count once. Encode each target FULL and actual already-selected Git base; choose prefix only if frame+32Bbase beatsFULL and selected graph fits50edges/64MiBcanonical/64MiBencoded. No candidate/codec sweep.

New authenticated large canonical objects use original LayerFS envelope with tagLFSWFL1\0 and rawbytes, identityBLAKE3(layerfs/object/v2\0||canonical). Existing small IDs remain identical. Scan each large rawversion using the frozen productCDC scanner; match nativechunkcanonicalIDs tosource. Select firstowner in sortedGitOID order, thenlowestoffset; nativechunks become original-ID authenticated bounded slice adapters to that retained largeobject. Authenticate the complete largeobject graph before taking a slice, then authenticate reconstructed originalchunkcanonical bytes. No adapter-to-adapter references. Unmapped native chunks stay physically FULL with unchanged original canonical identity; no missing object may be deleted. All original content IDs must remain indexed.

New unsupported physicalpack magicLFCNT1\0\0, version107, <=256records, u32offsetdirectory; kinds0/1smallFULL/prefix,2/3largeFULL/prefix,4native-slice(32Bowner+u32offset+u32length),5nativeFULL. Fixed2MiBlarge raw bound;2MiB+1KiBframe bound;4MiBnewpack bound; legacy metadata packs keep256KiBbound. Everyprefix record carries32Bcanonicalbase. NativeFULL raw<=32KiB. Slice owner must be a whole-filelargekind2/3 and offset+length<=ownerraw, length<=32KiB. Newframe parser checks checksum/content-size/dictID/window/trailingframes. Reader reconstructs at most51file graph nodes with64MiBcanonical+64MiBencoded closure; slice adds one <=32KiB canonicalchunk and one41Badapter. Per-reader canonicalcache4MiB, physicalpackcache8MiB, existingpoolcache4MiB; these payloadcaps are not processRSS. Dynamic pinnedzstd decoder uses declared max1MiBwindow and bounded input/output; runtime scratch/latency are unqualified. Record actual maximumclosures/readwork. Whole-file range-read amplification is an explicit cost, not hidden.

Authenticate every source originalcontentID, every new candidatecontentID and coverageadapter; crosscheck GitSHA1/selectedmanifest/oracles for allraw sourcefiles. Verify source/candidate metadata pack bytes and allnoncontent SQL/catalogue rows identical; originalobjects retained, extraobjects exactly224wholefileIDs. Build an actual fresh SQLitecopy, update contentlocators, delete only replacedcontentpacks, VACUUM, integrity/FKchecks, actual allocated/apparent/component accounting. Preserve sourcehash before/after. Export reader+actualcandidate for parent all53originalfilesystemoracles; contentauth alone is not thatproof. Keep allnegative outcomes. No productbuild/Docker/harnesschange/publicperformanceclaim.
