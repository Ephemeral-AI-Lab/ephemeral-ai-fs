# Frozen shared-inode-value protocol — 2026-09-10

Priority1 only. Baseline improved53 Store SHA256 ff2eb0bbff39cb822f5fe33514dd84adce817f2d86f4041296220211ba91a8f4,59,760,640 B with6,877,152 B metadata packs. Preserve all content bytes, IDs and all typed SQL history rows.

Pool each distinct73-byte inode value as canonical LFSIVL1 value object, full32-byte authenticated object ID in existing objects index. Deterministic ordinal is u32 position in fullhash-sorted pool, starting1. A separate SQLite table maps integer primary key ordinal to UNIQUE fullhash foreign key, retaining both real B-tree costs. Existing D inode leaves retain identical canonical IDs; their physical full form stores the original44-byte header plus serial8/ordinal4 entries. Reader expands values and authenticates every reconstructed original canonical leaf. Physical ordinals are locators, never truncated identities.

Use identical chronological leaf candidate choice, original canonical128KiB closure/16edge limit, original group membership and full-based pack boundaries, fixed bounded matcher and actual group threshold max(64,ceil(FULLencoded/8)). Matcher acts on physical shared-value leaf bytes. Pool objects are FULL-only, hash sorted, ≤16KiB decoded groups and≤256KiB packs. Existing objects table indexes every pool object; all retained bases counted once. No graph parameter sweep, no content/index optimization. Bound ordinal table population explicitly, original leaves≤100entries, each pooled value exactly94canonical bytes. On each chain expanded canonical closure≤128KiB; pool data read logical work≤192KiB per chain, excluding decompression-group amplification which is measured separately. Caches remain bounded.

Build actual matched VACUUM control and candidate DB53. Physically reconstruct all metadata and pool objects, authenticate content population and compare unchanged payload hashes; SQLite integrity/foreignkeys. If candidate is smaller, run all53original oracle checks and repeat samepolicy157 before accepting scaling claims. If candidate loses, reject after exact physical/canonical/semantic equality. Every attempt retained.

# Prospective full157 extension — 2026-09-10

Apply exactly the frozen53 shared-inode-value policy to the retained full157 structural copy, without tuning, because raw repeated-value ratio grows from4.60to9.89 across these histories. Initial53physicalallocation is66,527,232 B versus59,760,640control, a6,766,592 B regression; canonical checks remain running. Full157reference has79,790,080 B total and17,459,061 B metadata packs. SHA256 e5837e3484d561225fa464433ed94410705bdb6cde1f60b2c057faa834a73281. Only source paths/cardinalities/baselineconstants change. Poolhashes,ordinalwidth,indexschema,codec,candidatepolicy,group/packboundaries andreadbounds remainunchanged. This secondfixturetests fixedpoolcostscaling; it isnotaparametersearch. Reject if loss or unacceptable cold read amplification; no content or index experiment started.
