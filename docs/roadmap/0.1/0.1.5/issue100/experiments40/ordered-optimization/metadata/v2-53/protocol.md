# Prospective v2 physical value-pool catalogue — 2026-09-10

Separate Priority1 metadata representation, no retuning of v1 leaf or pool encodings. After the v1 validation gate, use its exact physical packs and ordinals. Remove the per-value logical CAS rows and ordinal/fullhash/UNIQUE tables; these physical values are not independently addressed by namespace roots. Store one indexed SQLite pool_groups row per physical value group: first ordinal (integer primary key), count, pack ID foreign key, group number, full32-byte SHA256 of decoded framed group. Exact v1 u32 ordinal is resolved by range lookup plus record slot. All group descriptors/index allocation and pack bytes count. No truncated hashes or omitted metadata: authenticate each group and every reconstructed original canonical leaf ID. Validate every pool record against v1 canonical data and all original namespace semantics.

Keep unchanged ordinary metadata/content canonical IDs, object locators, typed SQL history, all v1 leaf DELTA bases,16-edge128KiB canonical closure,192KiB logical pool work bound. Canonical cache4MiB, encoded pack cache8MiB, decoded pool group cache4MiB; report cold group read amplification separately. Pool canonical values are exactly94bytes including envelope; every decoded pool group≤16KiB, every pack≤256KiB. Ordinals aren't identities. Descriptor ranges must be contiguous and complete; catalogue-covered groups must exactly match removed pool locators, disjoint from ordinary CAS locators. One group SHA256 authenticates the physical group, full leaf canonical hash authenticates assembled logical values.

Build and check actual53copy first, then exactlysame157layout. Negative checks: missingordinal/group, wrongordinal, wrongvaluerole, badgroupdigest/corruptgroup, canonicalleafhash. If physicallysmaller than original improvedreference, parent performs alloriginal53/157oracles beforeacceptance. Keep v1results and allattempts. This policy changesmetadatarepresentation; existingcontent/indexstoragepolicy remains untouched.
