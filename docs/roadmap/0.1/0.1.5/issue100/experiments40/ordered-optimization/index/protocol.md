# Priority3 fixed SQLite page-size experiment — 2026-09-10

Source is the53-state content candidate, allocated54,382,592 B, SHA256 dd4a3c7cd2260b487b570cb819f2799ff189c2470382d4481b0f2fd19f3360a3. Perform one fixed change only: on a new copy set SQLite page_size=1024 and VACUUM, versus a matched4096-byte re-VACUUM control. No parameter sweep, codec/hash/length/index/schema/constraint modifications. Never overwrite source or prior attempts.

Authenticate source hash before/after. Compare complete schema and every logical row including every pack BLOB with deterministic typed streaming SHA256, separately for each table, and compare row counts. Require SQLite integrity and foreign-key checks. Report actual logical/allocated totals, dbstat table/index/overflow pages, payload/unused/overhead, and observed B-tree depth/page-count changes. SQLite internal schema rootpage numbers and page-size fields may change physically; logical schemaSQL and table/index definitions must not. All original identity/metadata/content fields remain exact.

A storage winner gets representative cold actual-reader checks covering namespace, metadata leaf, pooled value group, small/large file objects, native slice and native FULL fallback where populated, and selected original snapshot oracle checks. Exact complete logical equality permits carrying the previous all53-state content/metadata correctness proof; explicitly distinguish carried proof from fresh full-history reads. Parent may run all53 again. Full157 uses the exact same1024policy only after its verified source/hash are supplied. No public speed claim; smaller pages can increase B-tree traversal and overflow work.
