# Exact original-oracle verification for the assembled offline full157 copy

Read only the final combined diagnostic SQLite copy, not the source Store or a reconstructed source history. Resolve its newly derived commit IDs through the recorded old-to-new identity map and the original157 verification receipts. Walk each transformed namespace to enumerate all paths, types, mode values, logical lengths and content SHA256 values. Compare each complete state to `/Users/yifanxu/Ephemeral-AI-Lab/deepseek-history-data/oracles/<source-sha>.json`; verify source commit/index mapping against the pinned checkpoint manifest.

Every physical object read must reconstruct and authenticate the full canonical BLAKE3 ID. Metadata is decoded from the assembled copy's actual pack records. SmallContent uses the preserved diagnostic framing/dependency decoder; native chunk chains preserve kind, raw length, chronology and their fixed depth limits. Extent trees concatenate the exact referenced byte ranges. Symlink targets and portable modes come from their actual canonical records. No verification-only source content replaces candidate content.

Use the assembled-copy reader with a 4-MiB decoded canonical LRU and an 8-MiB encoded-pack LRU, plus per-content-root length/SHA summaries and locator metadata. File digest reuse is permitted only for identical authenticated content roots; every namespace still resolves and compares all paths/metadata. This is exact cryptographic content verification, not a throughput or process-memory measurement. No claim of public FUSE/SDK checkout, concurrent allocator, migration or transaction rollback.

Require157state PASS,904,143path states and4,936,693,030logical bytes matching the original same-Store receipts. Record perstate oracle hashes and observed summaries, source/copy before-after identities and script/helper hashes. Preserve first failure and stop; do not modify candidate data to satisfy an oracle. A first-state check may validate the verifier before the full157 pass; no repeat of the complete passing verification unless code or data changes invalidate it.
