# Full157 combined offline protocol

Written before full-corpus reencoding. The source is retained-full157-1/deepseek-full/host-runtime/store.sqlite, post-verification SHA256f323de0e0f9ae1030efc142402bc033ad427134dd8c69f21eb5b5d6ef7426eb7. It remains immutable read-only. This fixture contains157 committed snapshots plus one genesis namespace,207725 object locators,1663 v3 packs/75398 SmallContent objects,517 v2 packs/3221 native objects, and960 v1 packs/129106 metadata objects.

Freeze the same diagnostic representations already tested on ten states: D scoped8-byte inode IDs within namespace32-byte scope plus one global highwater allocator; full32-byte canonical object identities; SmallContent diagnostic framingB(version102) with4-byte starts and locator/range-derived lengths; the independently selected fixed chronological full157 CDC-family graph. No format search or parameter sweeps. Metadata and CDC agents supply exact authenticated exports with hashes and semantic checks.

Create one fresh complete source copy and VACUUM once to establish the full157 equally compact control. Preserve the control and measure its file SHA/logical/allocation/table geometry. Then create one final fresh source copy, replace all v1metadata with actual D inventory/pack/locator bytes, compact every v3pack with exact frame/kind/base conservation, and replace selected v2records while reconstructing directories. Keep native pack/group/ordinal locations if group<=65536 and pack<=262144 bounds hold; otherwise stop and report the boundary. Optional D+B intermediate is permitted only if CDC export materially delays useful progress; preference is one final combined copy.

Verify source typed LayerId/CommitId derivations, then rederive new layer and157 commit identities in DAG order and rewrite every dependent root/head/parent/base reference in one transaction with deferred foreign keys. Keep branch/stack IDs and names. Charge all metadata, scope allocator, fullhash index, packs and SQL state. Record158 root and typed-ID maps. External historical IDs necessarily change with canonical metadata encoding; semantic snapshots are the comparison contract.

Verify all selected physical locators have exactly one record and all records are selected, every metadata object matches authenticated export, all158 namespace semantic checks, all75398 SmallContent and3221 native targets/dependencies authenticate within current chain/closure bounds. Preserve content IDs and canonical lengths. Use bounded decode buffers; hold compressed records/metadata inventory if useful but do not cache gigabytes of decoded content. Expose a standalone StoreReader.read_canonical API with bounded cache for parent's independent full-file/type/mode/symlink oracle comparison. Parent owns independent path/extent semantic verification.

The copy must pass SQLite foreign-key/integrity checks, then VACUUM and close before logical and filesystem-allocation measurement. Reconcile metadata/small/native pack bytes, SQLite nonpack bytes, allocator/index/table pages and allocation difference. Compare with the separately verified existing matched157-snapshot Git baseline, reporting recorded and live Git allocation if they differ. Source SHA unchanged before/after. No product compatibility, online allocation, Save/Commit latency, branch/import concurrency, migration, rollback or historical API claims; unsupported diagnostic schema and magic refuse current product opening. No product build, Cargo/runtime writes, Docker or ten-state result mutation.
