# Stride3 native CDC replication: frozen before encoding

2026-09-10. Reproduce the full157 native lockfile similarity policy on the independently captured53-state Store; no alternative search or policy changes. Source Store `/Users/yifanxu/Ephemeral-AI-Lab/layerfs-issue100-stride3-comparison/layerfs/deepseek-stride3/host-runtime/store.sqlite`, SHA256 `5c6ee04eee133539f043ee64d242c26769c77d30b434af2523666e326a8d999e`. Read-only census:1998native objects,270packs,5,696,523pack bytes.

Fixture `/Users/yifanxu/Ephemeral-AI-Lab/layerfs-storage-v3-data/deepseek-stride3-218c1b81ed39c763ca22/fixture.json`, SHA256 `3b2c12b682e4892837fc810969e4e90eaa8d756c0d6f738df50746ad478fb73e`; profiledeepseek-stride3; exactly53 consecutive states corresponding originalindices1,4,...157. Recover actual lockfile CDC spans with existing scanner and all original byte/oracle seals. Check first admissions against THIS run's physical pack-range receipts and every selected prefix against THIS immediately preceding selected state's first-overlap hint; stop on missing objects/provenance mismatch.

Same unchanged policy:16byte windows/eight minhash signatures, at least2shared, highestoverlap thenlowestID, ONE previous-file winner plus firstoverlap,128entries/1MiB precedingfile, no cross-state index retention, max8cumulative lookups/512KiBtarget encoded+decoded work, depth4/rawclosure1MiB, unchanged zstd1.5.7 level3/window20/checksum/content-size parameters. Admit all first-lockfilechunk appearances in original physical order; update every selected representation before descendants. Include newlyFULL/worsened chunks. All outside-family native records retained. Count each final base and record once.

Authenticate every source native object before replacement, every replacement native object afterwards, and reconstruct every large selected lockfile version. Check original group/pack membership against64KiBgroup/256KiBpack bounds; any oversized pack reported to parent for actual repacking. Export `family-result.json:selected_graph` in existing rebuild_native schema. Parent builds actual completecopy and checks all53original metadata/content oracles. This subtask reports encoded/nativepack contribution only, no completeStore, Commit, resource-wave or productqualification claim. No productwrites/build/Docker/Git baseline modification.
