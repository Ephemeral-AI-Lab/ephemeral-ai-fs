# Issue102 campaign v1

Prospective declaration before implementation/collection. Use the existing
issue54 collector and family runners. Candidate starts at698ac8c95 with public
schema9 product seal24cde1dce88104daebf2d01e6b711665cfa52c52880b31157fbcfa5c27214673.
Released control is v0.1.4 (101fa273d815f3aaedb0e06ba0de7b0777d83def).
The offline9304 archive is not the runnable candidate.

## Matrix and gates

Freeze the live17-family host registry in `mandatory-registry.jsonl` and the
existing collector declaration in `mandatory-campaign.json`. It contains198
performance cases and29 proof-only definitions;226 routine proofs run, with
only the already declared600second sustained proof optional/unselected.
Add all11 historical_access v2 cases and11 separate proofs:209 performance
selections and237 routine proofs across18 mandatory families. Legacy capped-edit
routes outside HOST_FAMILIES are archival, not silently additional active cases.

Use seed/repetition1 and one full sample per active ordinary case; historical
access retains its declared seed0/repetition1. All cases retain exact public
operations, fixtures, resource and correctness gates. Existing family collection
uses300s product,310s command,600s setup; routine proof45s work/59s hard. The three
previously declared expensive proof preparations remain separately reported.
Historical access uses its single15s end-to-end envelope including preparation
and cleanup, with its own15s verifier watchdog. It never waits on a lock.

Freeze byte-identical harness/workload in control and candidate if their APIs
support this. Record compilation or schema incompatibility as INAPPLICABLE,
not PASS and not an invented baseline. Never alter the control product to make
it compile. Retained released results may provide contextual comparisons only
where exact semantics/timers/fixtures/topology are audited; changed harnesses
cannot substantiate a paired speedup claim. Historical-access's prepared schema9
Store is incompatible with released control: public access qualification is
candidate-only unless a separately authorized compatible input is available.

Initial broad collection is a diagnostic inventory, n=1 per case. Run control
then candidate for odd matrix rows and candidate then control for even rows where
paired applicability is established; retain every outcome. A >20% and >5ms
operation regression flags investigation, never a statistically certain claim.
Correctness, OOM/swap, cleanup, authentication, unsupported routes and deadline
failures take priority. Optimize only a measured shared cause. Confirm selected
changes with3 alternating matched pairs; retain only improvements without failed
correctness/resource gates or >20% and >5ms regressions in affected sibling calls.
No selected optimization is required if evidence does not justify one.

After changes stabilize, freeze exact source/build/fixture identities and run
one final complete mandatory matrix. Do not rerun passing cells on unchanged
artifacts for nicer numbers. If no relevant changes occur, the complete initial
exact-candidate run supplies terminal evidence; no redundant final rerun.
A failure remains a failure; no post-result budget or workload relaxation.

## Optional repository_history v1

Exactly3 explicit profiles: stride-1 indices1..157 (157states), stride-3
indices1,4,...157 (53states), stride-10 indices1,11,...151,157 (17states).
The common formula is sorted(set(range(1,158,stride)) | {157}). Preserve original
source/oracle identities and full157_index. Use existing sealed DeepSeek importer,
public Commit and full original-state verifier, with direct selected transitions
and no hidden commits. Freeze allocation before verification and retain the same
Store; fork bookkeeping may change its post-proof identity.

Require --profile for execution. --list/--self-check must not construct history.
No optional history is selected in this routine campaign; report each as
NOT_RUN_OPTIONAL. Profiles retain a separately declared14400s preparation/full
phase ceiling and300s per state/transfer/verification allowance. These ceilings
are not historical_access budgets. An explicit future run must pair its identical
selected Git/control history; there is no Git17 result yet. Do not relabel Git53
or Git157 as Git17. Reuse existing history report/allocated-storage machinery.

## Evidence and topology

SQLite/coordinator/canonical publication/spool stay on macOS. Linux Docker owns
daemon/FUSE/workload only; standard2CPU/2GiB/no swap/256PID limits. Reuse existing
build/preparation caches, never prune or hide mandatory setup. All phases use the
existing measurement lock; no overlapping builds/measurements.

Retain raw failures, commands, phase/outer wall, original timers, source/product/
harness/fixture/oracle/binary/image seals, sampled resources and cleanup receipts.
A final report enumerates every family/case/control applicability and optional
exclusion, with raw ns/bytes, per-case n, min/max/median and truthful scope.
Missing metrics stay unavailable with reasons. No release/tag/deployment.
